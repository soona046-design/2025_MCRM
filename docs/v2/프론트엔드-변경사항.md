# M-CRM v2 — 프론트엔드 구현 변경사항 (2026-07-11)

> 대상: `mcrm-v2/` (Next.js, 목데이터 기반)
> 근거: [기능명세서.md](기능명세서.md) · [IA-화면목록.md](IA-화면목록.md) — 2026-07-11 manyfast PRD 갱신분 반영
> 상태: **검증 완료 · 커밋됨** (2026-07-11). 빌드·타입체크 통과 + 브라우저 실클릭 검증 완료 — 매출 직접 입력(신규 유입 검색연결→등록→목록 반영), 기존 고객 신규 등록 중복 경고, 단계 역행 사유 필수(이력 기록), URL 붙여넣기 UTM/클릭ID 자동 파싱(확정 귀속 배지 전환), 채널→캠페인 드롭다운 연동, 업로드 5단계·금액 컬럼 제거, 기존 고객 목록/상세, 목표 스코프 탭, 분석 이중축·기여도 안내, 대시보드 "지금 수집"(실패→지연 전환)까지 전부 동작 확인. 콘솔 에러 0건.
> **2026-07-12 폴리싱**: 경미 관찰 2건 모두 원인 파악 후 수정 완료 — 상세는 §6 참고.
> **2026-07-12 스펙 정합화**: 명세서 자체의 내부 모순(초반 요약표 vs §1.3.8 상세 스펙) 발견·해소. 메인
> 대시보드 KPI 카드에 누락돼 있던 ROI(%) 수치를 추가하고, 대시보드 ROAS 차트를 상세 스펙이 요구하는
> 단일축(+광고비 배경 막대)으로 되돌렸으며, 이중축 ROAS·ROI 비교 차트는 §7.3.1이 지정한 제자리
> (`/revenue/analytics`)에만 남기고 그 화면에 빠져있던 광고비 막대·인터랙티브 툴팁을 채웠다.
> 상세는 §7 참고.
> **2026-07-12 대시보드 인사이트 강화**: 피그마 유저플로우 대조에서 채택 가치가 확인된 3건 구현 — 퍼널
> 이탈 집중 구간 자동 하이라이트, 대시보드 상단 목표 달성률 요약 카드, ROAS·전체 매출 카드의 전년
> 동기(YoY) 비교 배지. 작업 중 발견한 ROI 배지 오류(당월 부분 실적 ÷ 월 전체 광고비 → -335.7%p)도
> 광고비 pace 보정으로 수정. 상세는 §8 참고.

---

## 1. 핵심 변경 요약

| 변경 | 이전 | 이후 |
|---|---|---|
| 퍼널 단계 | 문의 → 상담 → 예약 → 계약 (4단계) | 유입 이벤트 → 리드 → 상담 → 견적/제안 → 계약 (5단계) |
| 계약 금액 입력 위치 | 고객 상세 화면(유입 관리) | **매출 관리 전용** — 유입 관리에서는 입력 UI 자체가 없음 |
| 매출 지표 | ROAS 단일 (계약 금액 전체 ÷ 광고비) | **ROAS**(신규 유입 고객 매출만) / **ROI**(신규+기존 전체 매출) 분리, 라벨 상시 병기 |
| 고객 구분 | 없음 | **신규 유입 고객**(유입 관리 연결) vs **기존 고객**(재계약·직거래) |
| 광고 매칭 증거 입력 | UTM/클릭ID 수기 입력만 | + 캠페인 드롭다운 자동연동, URL 붙여넣기 자동파싱 |
| 수집 모드 표현 | (미정) | 정기 수집(06:00) / **수동 재수집**("지금 수집" 버튼) 2모드 |
| 화면 수 | 20개 | **21개** — 기존 고객 목록/상세(D5) 신규 |

---

## 2. 신규 파일

| 파일 | 내용 |
|---|---|
| `src/lib/mock-existing-customers.ts` | 기존 고객 목데이터 스토어. 검색(`searchExisting`), 중복 후보 탐지(`findDuplicateCandidate`), 신규 등록(`addExistingCustomer`) |
| `src/lib/utm.ts` | `parseUtmUrl()` — 붙여넣은 URL/쿼리스트링에서 utm_source/medium/campaign, 클릭ID(gclid/gbraid/wbraid/nclk) 자동 추출 |
| `src/components/revenue/contract-ui.tsx` | `ContractStatusBadge`, `CustomerTypeBadge`, `EditAmountDialog`(금액 인라인 편집 — ROAS/ROI 재계산 안내) — 계약 목록·기존고객 상세 화면 공용 |
| `src/app/(main)/revenue/existing/page.tsx` | **D5** 기존 고객 목록 — 검색/담당자 필터, 마지막 계약일·누적 매출액 정렬 |
| `src/app/(main)/revenue/existing/[id]/page.tsx` | **D5** 기존 고객 상세 — 누적 매출 요약, 다중 계약 이력 표, 재계약 주기 안내 |

## 3. 수정 파일

### 데이터 레이어
- **`src/lib/mock-customers.ts`** — `Stage` 타입을 5단계로 교체, `Customer`/`NewCustomerInput`에서 `contractAmount` 제거, 광고 채널·캠페인 드롭다운 데이터(`AD_CHANNELS`, `CAMPAIGNS_BY_CHANNEL`) 추가
- **`src/lib/mock-revenue.ts`** — 전면 재작성. `contracts`가 계약 금액을 단독 소유(고객 스토어에서 파생하지 않음), `customerType`(신규 유입/기존) 필드, `addContract`/`updateContract`(이력 기록), `roasRoiTrend()`(이중축 추이), `GOAL_SCOPES`(고객유형별 목표)

### 고객 유입 관리
- **`customers/[id]/page.tsx`** — 계약 금액 카드·다이얼로그 제거 → "매출 관리에서 계약 금액 등록" 링크로 대체. 단계 역행 시 사유 선택(재검토 필요/오류 수정/기타) 필수화
- **`customers/new/page.tsx`** — 계약 금액 필드 제거. 광고 채널 선택 시 캠페인 드롭다운 자동 연동(목록에 없으면 직접 입력 전환), "URL 붙여넣기" 입력란으로 UTM/클릭ID 자동 파싱
- **`customers/upload/page.tsx`** — `STAGE_ALLOWED` 5단계로, `contractAmount` 컬럼·검증·템플릿/샘플 CSV에서 전부 제거

### 매출 관리
- **`revenue/page.tsx`(D1)** — 고객유형 탭(전체/신규 유입/기존), "매출 직접 입력" 다이얼로그(신규=유입 고객 검색연결, 기존=자동완성 검색 또는 신규 등록+중복 병합 확인), 금액 클릭 시 인라인 편집, 목록에 고객유형 컬럼 추가
- **`revenue/analytics/page.tsx`(D4)** — 고객유형 필터 탭, ROAS(좌축)·ROI(우축) 이중축 차트 추가, 채널 기여도는 신규 유입 전용(기존 선택 시 안내 문구)
- **`revenue/goals/page.tsx`(D3)** — 목표를 전체/신규 유입/기존 탭으로 세분화, 이력에 구분 배지 표시
- **`components/revenue/revenue-tabs.tsx`** — "기존 고객 목록" 탭 추가

### 대시보드(B1)
- **`components/dashboard/kpi-cards.tsx`** — "계약 금액" 카드 → "전체 매출"(전체 포함 라벨)로 교체, ROAS 카드에 "신규 유입 기준" 라벨 추가
- **`components/dashboard/roas-trend.tsx`** — 채널별 ROAS 추이 → **ROAS·ROI 이중축 추이**로 전면 교체(`roasRoiTrend()` 사용, 실제 계산값 반영)
- **`components/dashboard/funnel-chart.tsx`** — 5단계(유입 이벤트→리드→상담→견적/제안→계약)로 갱신
- **`app/(main)/page.tsx`** — 레이아웃을 명세 순서(요약카드 → ROAS·ROI 추이 → 채널비교+퍼널 → 채널비중 → 신뢰도)로 재배치, "지금 수집"(수동 재수집) 버튼 추가

### 내비게이션
- **`components/layout/app-shell.tsx`** — 사이드바 "매출 관리" 하위에 "기존 고객 목록" 추가, 상세 페이지 활성 상태 매칭 규칙 보강

---

## 4. 반영하지 않은 항목 (범위 밖)

- **백엔드**: v2 Laravel 신규 프로젝트는 아직 미착수(VPS 등 인프라 결정 필요, 별도 확인 후 진행)
- **B2/B3(채널·캠페인 상세 분석), E1~E4(보고서), F1~F5(설정)**: 기존에도 목데이터 스캐폴드가 없던 화면이라 이번 범위에서 신규 구축하지 않음 — 사이드바에 "준비 중"으로 표시된 채 유지
- **브라우저 인터랙션 테스트**: 이 세션에서 Claude in Chrome 확장이 연결되지 않아 폼 제출·다이얼로그 클릭 등 실제 조작 확인은 못 했음. curl 기반 SSR 콘텐츠 검증(라우트 200, 신규 문구 노출)까지만 완료

## 5. 확인 방법

```bash
cd mcrm-v2
npm run dev   # http://localhost:3000
```

주요 확인 포인트:
- `/customers/new` — 광고 채널 선택 → 캠페인 드롭다운, URL 붙여넣기 자동파싱
- `/customers/[id]` (계약 단계 고객) — 계약 금액 입력란이 없고 매출 관리 링크만 노출
- `/revenue` — "매출 직접 입력" 다이얼로그, 고객유형 탭, 금액 인라인 편집
- `/revenue/existing`, `/revenue/existing/[id]` — 신규 화면
- `/` — KPI 카드 "전체 매출"(ROI% + 전월 대비 배지 포함), ROAS 단일축 추이(+광고비 배경 막대), "지금 수집" 버튼
- `/revenue/analytics` — ROAS·ROI 이중축 차트(광고비 배경 막대 + 포인트 클릭 시 ROAS/ROI/매출/광고비 툴팁)

---

## 6. 2026-07-12 폴리싱 — 경미 관찰 2건 조치

### ① 기존 고객 상세 "재계약 주기 안내" 일부 미노출
- **원인**: `revenue/existing/[id]/page.tsx`가 "오늘" 기준으로 `new Date()`(실제 시스템 시각)를 썼는데, 목데이터의 계약일은 전부 2026년으로 고정돼 있어 실제 시계와 어긋나면 경과일 계산이 항상 0 또는 비정상적으로 커짐 — 다른 화면(`mock-customers.ts`의 `NOW_REF`, `customers/page.tsx`의 `NOW`)은 전부 앱 내부 고정 기준시각을 쓰는 것과 불일치했던 게 근본 원인.
- **조치**: `mock-revenue.ts`가 이미 내보내는 `CURRENT_MONTH`(`2026-07`)·`DAYS_ELAPSED`(`10`)로 고정 기준일(`2026-07-10`)을 구성해 사용하도록 수정. 부수적으로 `daysBetween`의 불필요한 `Math.max(0, …)` 클램프도 제거(미래 날짜 계약과의 차이를 자연스럽게 음수로 표현해 조건이 올바르게 `false`가 되도록).
- **부가 조치**: 이 기준일로 재계산하면 목데이터 생성 규칙상 최근 계약이 항상 "충분히 최근"이라 배너가 사실상 아무에게도 안 뜨는 2차 문제가 드러나, `mock-revenue.ts`의 기존 고객 계약 시드에서 계약 2건 이상 고객의 절반(`ei % 2 === 0`)은 마지막 계약을 의도적으로 3개월 더 이전으로 배치하도록 조정. 계약 1건 고객은 애초에 주기를 계산할 수 없어 항상 미노출인 것이 정상 동작.
- **검증**: `ex1000~ex1011` 12명 중 `ex1002·ex1004·ex1008·ex1010`(계약 2건 이상 + 오래된 마지막 계약)에서만 배너 노출 확인, 나머지는 미노출 — 데이터 조건부 동작임을 확인.

### ② ROAS·ROI 차트 당월 라벨 겹침
- **원인**: 당월(7월) ROAS가 급락(0.45배)하고 ROI가 마이너스(-17.8%)로 나오면서 두 값의 화면상 y좌표가 서로 가까워졌는데, 라벨 위치를 각 포인트 기준 고정 오프셋(`-8`/`+16` 등)으로만 계산해 겹침 방지 로직이 없었음.
- **조치**: `components/dashboard/roas-trend.tsx`와 `revenue/analytics/page.tsx`의 `RoasRoiChart` 두 곳 모두에 최소 간격(14~16px) 보장 로직을 추가 — 두 라벨이 너무 가까우면 아래쪽(ROI) 라벨을 밀어내고, 캔버스 하단을 벗어날 경우엔 위쪽(ROAS) 라벨을 대신 밀어 올리는 방식으로 항상 최소 간격과 캔버스 내부 범위를 보장.
- **검증**: 렌더링된 SVG에서 두 라벨의 실제 `y` 좌표 차이가 대시보드 43px, 분석 화면 50px로 벌어져 있음을 확인(둘 다 `MIN_GAP` 이상, 캔버스 범위 내).

## 7. 2026-07-12 스펙 정합화 — ROAS·ROI 대시보드 배치 오류 수정

### 배경: 명세서 자체의 내부 모순
- **초반 요약표** (§1, 개요 수준 표): "2행: ROAS·ROI 기간별 추이 — 이중축 라인 차트"
- **§1.3.8 상세 스펙** (더 구체적, 더 나중에 작성됨): 1행 카드에 "전체 매출(ROI 기준)" 카드를 별도 배치하고,
  2행은 **ROAS 단일축 차트 + 광고비 배경 막대**로 명시. 그리고 못박음: "ℹ️ ROI(전체 매출 기준)는 1행
  요약 카드에서 확인하세요. 보고서의 ROAS·ROI 비교 차트에서 두 지표를 함께 볼 수 있습니다."
- **§7.3.1** (매출 관리 › 추이·기여도 분석, `/revenue/analytics`): 이중축 ROAS·ROI 비교 차트의 정식 스펙
  (좌 ROAS·우 ROI, 광고비 배경 막대, 클릭 시 4값 툴팁, 데이터 누락 회색 점선)이 여기 있음

기존 구현은 초반 요약표만 따라가 이중축 차트를 메인 대시보드에 넣었고, §1.3.8이 요구한 "전체 매출 카드의
ROI(%) 표시"는 누락돼 있었음. 상세 스펙이 배치를 명확히 이원화하고 있어 §1.3.8/§7.3.1을 따르는 쪽으로
정합화(사용자 확인 후 진행).

### 조치
- **`kpi-cards.tsx`**: "전체 매출" 카드에 ROI(%) 수치 + 전월 대비 증감 배지(%p) 추가, 라벨을 "전체 매출
  기준"으로 고정 표기 (ROAS 카드와 분자가 다름을 항상 구분)
- **`mock-revenue.ts`**: `roasRoiTrend()`가 `adCost`·`newLeadRevenue`·`totalRevenue`도 함께 반환하도록
  확장, 전월 대비 ROI 증감을 계산하는 `roiMoMDelta()` 신규
- **`components/dashboard/roas-trend.tsx`**: 이중축 → **ROAS 단일축 + 광고비 배경 막대**로 교체. 제목
  "ROAS 기간별 추이 (신규 유입 고객 기준)"
- **`revenue/analytics/page.tsx`의 `RoasRoiChart`**: 이중축 차트를 §7.3.1 요구사항에 맞춰 보강 —
  광고비 배경 막대 추가, 포인트 클릭 시 ROAS/ROI/매출/광고비 4값을 동시에 보여주는 인터랙티브 툴팁 패널
  추가(hover + click 모두 지원)
- **미반영 항목**: §7.3.1의 "데이터 누락 구간 회색 점선 표시"는 목데이터에 결측 월이 없어 실제 렌더링
  검증은 하지 못함 — 실 데이터 연동(M3) 시 확인 필요

### 검증
- 클린 빌드(`rm -rf .next && next build`) + `tsc --noEmit` 통과
- 브라우저 실클릭 검증: 대시보드 KPI "전체 매출" 카드에 "ROI -17.8%" + "▼ 335.7%p" 배지 노출 확인,
  ROAS 단일축 차트에 광고비 배경 막대 렌더링 확인, `/revenue/analytics`에서 1월 포인트 클릭 →
  툴팁 패널이 "1월 · ROAS 2.50배 · ROI 250.0% · 매출 ₩24.5M · 광고비 ₩7.0M"으로 즉시 갱신 확인
- 콘솔 에러 0건(새 브라우저 탭 기준 — 기존 탭에는 여러 차례 코드 수정을 거치며 쌓인 stale HMR 번들
  경고가 남아있었으나 실제 버그 아님, 새 탭·클린 리로드로 재현 안 됨을 확인)

## 8. 2026-07-12 대시보드 인사이트 강화 — 피그마 대조 채택 3건 + ROI 배지 버그 수정

### 배경
피그마 유저플로우(2026-07-11 작성분)를 현재 구현·명세서와 3자 대조한 결과, 피그마 플로우 자체는
7/11 PRD 갱신 **이전** 규격(4단계 퍼널, ROI 미분리 등)이라 대부분 무시했으나, 피그마에만 있고 구현에는
없던 **인사이트 표현 3건**은 명세(§1.2.2 성과 저하 인사이트, §7.2.2 달성률, §7.3 전년 동기 증감)와
합치해 채택했다.

### 조치 (신규 파일 1 + 수정 3)
- **`components/dashboard/funnel-chart.tsx`** — 각 단계에 전월 대비 전환율 증감(%p) 데이터를 부여하고,
  가장 크게 하락한 전환 구간을 자동 감지해 ① 해당 전환율 수치를 빨간색으로, ② 카드 하단에 "🔴 이탈 집중
  구간: A→B 전환율 N% — 전월 대비 -N%p 하락" 경고 배너로 표기. (worstIdx는 하드코딩이 아니라 배열에서
  계산 — 데이터 교체 시 자동 이동)
- **`components/dashboard/goal-progress-card.tsx`** (신규) — 대시보드 최상단(KPI 카드 위) 목표 달성률
  요약. 이번 달 매출/목표/달성률·월 경과 세로선이 있는 진행 막대·월말 예측(미달 시 "미달 우려" 빨간
  표기). 전체 스코프 기준이며, 스코프별 상세는 매출 관리 › 추이·기여도 분석으로 위임.
- **`components/dashboard/kpi-cards.tsx`** — ROAS 카드와 전체 매출 카드에 "전년 동기 대비" 배지 추가.
  ROAS 헤드라인 수치·전월 대비 배지를 정적 목업값(3.2배/+0.4)에서 실제 계산값(`roasRoiTrend()`/
  `roasMoMDelta()`)으로 연결해 다른 지표와 정합.
- **`lib/mock-revenue.ts`** — `yoyDelta()`(전년 동월 시드 대비 매출·ROAS 증감, pace 보정), `roasMoMDelta()`
  신규.

### ROI 배지 버그 수정 (§7에서 노출된 -335.7%p의 실제 원인)
§7 검증 당시 "전체 매출" 카드 전월 대비 배지가 **-335.7%p**로 찍혔는데, 이는 당월(10일차) **부분 실적**을
**월 전체 광고비**로 나눠 ROI가 비정상적으로 낮게(-17.8%) 계산된 결과였다. `roasRoiTrend()`의 당월 행이
광고비를 경과일 비율(`DAYS_ELAPSED/DAYS_IN_MONTH`)만큼 환산(pace 보정)하도록 고쳐, ROAS·ROI 모두
"현재 속도" 기준으로 앞선 달들과 같은 선상에서 비교되게 했다. 이제 대시보드와 `/revenue/analytics`가
동일 값(ROAS 1.40배 / ROI 154.8%)을 표시한다.

### 검증
- `tsc --noEmit` 통과
- 브라우저 실측(1440px): 목표 달성률 카드 "19.6% · ≈₩25.5M 미달 우려", ROAS 카드 "1.40배 ▼-1.64 ·
  전년 동기 대비 -0.8배(-37.2%)", 전체 매출 카드 "ROI 154.8% · 전년 동기 대비 +20.2%", 퍼널 하단
  "🔴 이탈 집중 구간: 상담→견적/제안 전환율 46.6% — 전월 대비 -8.2%p 하락" 노출 확인. 콘솔 에러 0건.

## 9. 2026-07-12 B2·B3 채널/캠페인 상세 분석 화면 (M3 프론트)

대시보드 하위 2개 화면을 목데이터로 신규 구현. 사이드바에서 "준비 중"(href 없음)이던 자리를 실제
라우트로 연결하고, 3화면 간 이동 탭(`DashboardTabs`)을 통합 요약 화면에도 추가했다.

### 신규 파일
- **`lib/mock-analytics.ts`** — 채널×월 지표(노출·클릭·광고비·전환·확정귀속/소스미상 매출·완결도) 생성기와
  캠페인·키워드 시드. ROAS 분자는 확정 귀속 매출만. 당월은 경과율(0.32) 반영한 부분 실적.
- **`app/(main)/dashboards/channels/page.tsx`** (B2, §1.2·§1.3.6, S-RJQSXZ) — 기간 필터(이번 달/최근
  3·6개월), 채널×기간 검증용 상세표(확정귀속·소스미상 분리, 합계행), 메타 수집 실패 시 회색 행·합계
  제외, 채널별 월간 광고비 스택 막대(표+그래프 병행), "이 조건으로 보고서 만들기" CTA(E2 준비 중이라
  현재는 필터 인계 toast).
- **`app/(main)/dashboards/campaigns/page.tsx`** (B3, §1.2.1·§1.3.2, S-XIEUKQ) — 캠페인 ROAS 순위표,
  입력률·UTM 매칭률·귀속률 병기(85%↓ 경고색), 검색광고 캠페인 행 클릭 시 키워드 효율(클릭/CPC/전환율)
  펼침. 메타·디스플레이는 키워드 데이터 없음 안내.
- **`components/dashboard/dashboard-tabs.tsx`** — 통합 요약/채널별 상세/캠페인별 상세 이동 탭(RevenueTabs 패턴).

### 수정 파일
- **`components/layout/app-shell.tsx`** — 대시보드 하위 메뉴를 "채널 리포트/전환 분석"(placeholder)에서
  "채널별 상세/캠페인별 상세"(실 라우트)로 교체.
- **`app/(main)/page.tsx`** — 상단에 `DashboardTabs` 추가.

### 검증
- `tsc --noEmit` 통과, 콘솔 에러 0건
- 브라우저 실측: B2 최근 3개월 합계 "광고비 ₩27.37M(메타 제외) · ROAS 3.07배", 메타 행 "수집 실패·
  ROAS —" 확인. B3 ROAS 내림차순(4.00→2.00배), 메타 캠페인 매칭·귀속률 경고색(71/58%, 68/52%),
  "7월_임플란트_브랜드" 행 클릭 시 키워드 3개(임플란트 가격 등) 펼침 확인.
- 미반영: 보고서 CTA는 E2(보고서 생성) 화면이 아직 없어 toast로 대체 — M5에서 필터 자동 인계(S-LMUKBI) 연결 예정.
  → **§10에서 해소**: E2 구현과 함께 CTA를 `/reports/new?from=…&channels=…` 라우팅으로 교체.

## 10. 2026-07-12 M5 보고서 화면 E1~E4 (목록·생성·뷰어·버전 비교)

보고서 4화면을 목데이터로 신규 구현. 사이드바 "준비 중"이던 보고서 메뉴를 실 라우트로 연결하고,
B2·B3의 "이 조건으로 보고서 만들기" CTA를 보고서 생성 화면 필터 인계로 교체(§9 미반영 항목 해소).

### 신규 파일
- **`lib/mock-reports.ts`** — 버전 시드(실패 버전 1건 포함), 생성 이력·저장 필터를 모듈 스코프 배열로
  유지. `generateReport`는 최신 요약값에서 소폭 변동한 새 버전을 append. `saveFilter`(최대 10개, 이름 중복 차단).
- **`components/reports/report-tabs.tsx`** — 보고서 목록/생성 이동 탭.
- **`app/(main)/reports/page.tsx`** (E1, §3.3.3) — 버전/종류/조회기간/필터/생성자/생성일시 목록, 실패 버전
  제외, "데이터 업데이트됨" 배지, 행 클릭 → 뷰어, 비교 링크.
- **`app/(main)/reports/new/page.tsx`** (E2, §3.2·§3.3.1) — 종류(주/월/연)·채널·캠페인·지표 필터, 저장
  필터 목록·저장 다이얼로그, 대시보드 인계 배지(`?from=`)+조건 초기화, 메타 실패 시 누락 경고(포함/제외
  선택), 생성 스피너 "보고서를 만들고 있어요" → 완료 시 미리보기/PDF/Excel. `useSearchParams`는 Suspense로 래핑.
- **`app/(main)/reports/[id]/page.tsx`** (E3, §3.1·§3.3.2) — 종류별 템플릿 섹션 렌더(주간: 채널막대→퍼널
  →ROAS·전체매출 카드 / 월간: KPI 4종→채널상세·지출도넛→캠페인ROAS→퍼널 / 연간: ROAS·ROI요약→채널비중),
  신뢰도 상태바·주석, 사내 보고 고지 문구 고정, PDF/Excel 버튼(현재는 toast).
- **`app/(main)/reports/compare/page.tsx`** (E4, §3.3.3) — 최신 vs 선택 버전, 주요 지표(ROAS·광고비·계약
  금액) 증감 하이라이트(증가 파랑↑/감소 빨강↓, 광고비는 중립), 비교 대상 드롭다운.

### 수정 파일
- **`components/layout/app-shell.tsx`** — 보고서 하위 메뉴를 실 라우트로 연결, `childActive`에 뷰어·비교
  경로(`/reports/[id]`·`/reports/compare`)를 목록 메뉴 귀속으로 추가.
- **`app/(main)/dashboards/channels/page.tsx`·`campaigns/page.tsx`** — CTA를 toast → `/reports/new?from=…&channels=…` 라우팅으로 교체.

### 검증
- `tsc --noEmit` 통과, 콘솔 에러 0건
- 브라우저 실측: E1 목록 4건(실패 v3 제외, v6 "데이터 업데이트됨" 배지). E2 대시보드 인계 배지
  "네이버 검색, 구글" 채널 인계+캠페인 필터 정합, 생성 idle→"보고서를 만들고 있어요"→"보고서가
  준비되었어요" 상태 전이 확인. E3 월간 뷰어 전 섹션(KPI/채널막대/도넛/캠페인ROAS/퍼널)+사내 고지 렌더.
  E4 v4↔v6 비교 ROAS +5.8%·계약금액 +6.7% 파란 증가, 광고비 중립, 주요 지표 태그 확인.
- 한계: 생성한 새 버전(v7)은 hard reload 시 모듈 상태 초기화로 사라짐(기존 목스토어 공통 동작). SPA 내
  Link 이동에서는 유지. PDF/Excel 실제 내보내기·역할별 마스킹은 백엔드 연동(M3+) 대상.

## 11. 2026-07-12 M6 — 온보딩 A2~A4 + 설정 F1~F5 + 알림 센터

1차 출시 22개 화면 중 마지막 마일스톤(M6). 사이드바 "준비 중"이던 설정 메뉴 전체를 실 라우트로
연결하고, 헤더 알림벨을 알림 센터와 실시간 동기화. 이 세션은 별도 창(데스크톱 앱)에서 M1~M5가
진행되는 동안 병행 작업 — 겹치지 않는 M6 범위만 담당.

### 신규 파일
- **`lib/mock-settings.ts`** — 채널 연동 상태(토큰 만료 D-5 시나리오)·실패 이력 90일·외부 매출 연동
  설정, 역할별 기본 권한 매트릭스(§2.1.4)+아이디별 오버라이드(`effectivePermission`), 개인정보 보관
  정책·주간 처리 리포트, 임계치(채널 실패 20%·입력률 90/70%·목표 70%·매칭 관대/엄격), 알림 5건 시드,
  온보딩 상태(모듈 스코프). 알림 읽음 상태는 `subscribeNotifications`/`getUnreadNotificationCount`
  최소 pub-sub로 외부 공개 — 헤더 배지가 `useSyncExternalStore`로 구독.
- **`app/(main)/onboarding/page.tsx`** (A2~A4, §6) — 3단계 마법사. 1단계 채널별 연결 시뮬레이션(0.9s
  스피너)+연결 방법 다이얼로그+건너뛰기 확인("미설정 채널 N개"), 2단계 템플릿 다운로드(csv-import 재사용)
  +테스트 업로드, 3단계 체크리스트 3항목+대시보드 활성화 판정(전체/부분/비활성).
- **`app/(main)/settings/channels/page.tsx`** (F1, §1.1.1·§1.3.7) — 채널 카드(연결됨/실패/토큰 만료
  임박 D-7 경고+재연결), 실패 이력 표(미해결/복구됨), 외부 매출 연동(웹훅/API 폴링/파일, fallback 선택).
- **`app/(main)/settings/users/page.tsx`** (F2, §2.1.4~5) — 사용자 목록·역할 드롭다운·개별 설정 배지,
  기능별 허용/차단/기본값 3상 토글 다이얼로그, 변경 시 불변 이력 자동 append.
- **`app/(main)/settings/privacy/page.tsx`** (F3, §2.1.6) — 데이터 종류별 보관 개월 인라인 편집·만료 시
  처리(익명화/삭제) 배지, 주간 처리 리포트(예정/완료/실패/재시도)+실패 건 재시도.
- **`app/(main)/settings/system/page.tsx`** (F4) — 임계치 4종 편집+매칭 키 엄격/관대 모드 토글.
- **`app/(main)/notifications/page.tsx`** (F5) — 카테고리 배지(수집 실패/입력 지연/목표 미달/소급 반영/
  토큰 만료)별 색상, 카테고리→관련 화면 딥링크(채널 연동/미입력 현황/추이 분석), 모두 읽음.
- **`components/dashboard/onboarding-banner.tsx`** (§6.3 수용 기준 3) — 체크리스트 미완료 시 대시보드
  상단 경고 배너("지금 설정 완료하기" CTA), 부분 활성화 안내 문구, 완료 시 자동 숨김.

### 수정 파일
- **`components/layout/app-shell.tsx`** — 설정 하위 5개 메뉴 실 라우트 연결(광고 채널 연동/사용자 권한/
  개인정보 정책/시스템 설정/온보딩 재시작), 헤더 알림벨 button→Link(`/notifications`), 하드코딩 배지
  "2" → `useSyncExternalStore` 실시간 미읽음 수(0이면 숨김).
- **`app/(main)/page.tsx`** — `OnboardingBanner` 삽입 (메타 실패 배너 위).

### 검증
- `tsc --noEmit` 통과, 콘솔 에러 0건
- 브라우저 실측: 온보딩 1→2→3 전체 플로우(네이버 연결 "연결됨 ✔"+카운터 1/3, 테스트 업로드 "5건 성공
  확인", 체크리스트 3항목 완료 시 "전체 활성화"). 대시보드 초기 상태에서 온보딩 배너 노출 → 완료 후
  재진입 시 배너 소멸 확인. F2 권한 다이얼로그에서 "스프레드시트 업로드 차단" 클릭 → 목록 "2건 개별
  설정" 배지·변경 이력 즉시 반영. 알림 센터 "모두 읽음" → 헤더 벨 배지 실시간 소멸(수정 전엔 미동작
  버그 — pub-sub 도입으로 해결).
- 한계: OAuth 연결·토큰 검증은 시뮬레이션(M3 백엔드 연동 대상). 온보딩 상태는 모듈 스코프라 hard
  reload 시 초기화(기존 목스토어 공통 동작). A1 로그인 화면은 인증 방식 결정 후 별도 작업.

## 12. 2026-07-12 M3/B1 — 백엔드 신설 + 고객 유입 관리(C1~C5) 실 API 전환

**새로고침하면 데이터가 사라지는 문제 해소** — 목스토어(브라우저 메모리)를 실제 DB(SQLite) 기반
Laravel API로 교체. 배포 환경 결정은 보류(로컬 개발 먼저), 설계 근거는 `백엔드-설계.md`.

### 백엔드 (신규 저장소 `mcrm-v2-backend/`, 중첩 git)
- Laravel 13 + SQLite(파일 영속, 데몬 불필요 — 배포 시 MySQL 전환 가능), KST 타임존.
- 스키마(설계 §3.1·§3.3): `users`(uuid+role), `customers`(5단계 stage·stage_order), `attribution_evidences`
  (captured_via), `change_logs`(불변 — 모델 update/delete 예외 발생), `inflow_events`(입력률 KPI 분모).
- API(설계 §4 계약): 목록(q·stage·source·owner·sort·page + 단계 칩 카운트)/상세(이력 포함)/수기 입력
  (금액 필드 없음, 증거 유무→confirmed/unknown 판정)/단계 변경(역행 사유 이력)/담당자 변경/
  `GET /overdue`+`/overdue/metrics`(최근 30일 입력률).
- 저장은 영문 enum, 응답은 한글 라벨(`app/Support/KoLabels.php` 단일 변환 소스) — 프론트 화면 코드
  거의 무변경으로 전환.
- 시더가 목스토어 생성 규칙을 그대로 이식(고객 47·지연 8·사용자 4) → 전환 전후 화면 동일.
- 실행: `cd mcrm-v2-backend && php artisan serve --port 8100` (launch.json 등록됨).
  초기화: `php artisan migrate:fresh --seed`.

### 프론트 (`mcrm-v2`)
- **`lib/customers-api.ts`** 신규 — mock-customers와 동일 타입 재수출 + fetch 클라이언트
  (기본 http://localhost:8100/api, `NEXT_PUBLIC_API_URL`로 재정의).
- C1 목록: 서버 필터·정렬·페이지네이션, 검색 250ms 디바운스, 로딩/서버 다운 안내 행.
- C2 상세: 서버 응답으로 화면 갱신 — 단계 역행(사유 포함)·담당자 변경이 불변 이력으로 기록.
- C3 수기 입력·C4 업로드: `POST /api/customers`(업로드는 행 단위 순차 + 실제 진행률, 실패 건수 토스트).
- C5 미입력 현황: 서버 집계(입력률·30일 분모·지연 건수).

### 검증 (브라우저 실측)
- `tsc --noEmit` 통과, 콘솔 에러 0건.
- 수기 입력 등록(POST 201) → 목록 반영 → **하드 리로드 후 유지** 확인(핵심 수용 기준).
- 역행 변경(계약→상담, 사유 '재검토 필요')이 서버 시각·행위자(김민준·팀장)로 이력 기록.
- curl: 목록/등록/역행/담당자/미입력 목록·메트릭(92.7%) 전 엔드포인트 정상.
- 한계: 인증 없음(A1 확정 전 공개 라우트, 행위자는 시드 팀장 고정). 업로드 배치 커밋 API·대시보드/
  매출/보고서 실데이터 전환은 B2~B4. 광고 API 커넥터(B3)는 **네이버/메타/구글 API 심사 선신청 필요**.

## 13. 2026-07-12 M3/B2 — 매출·목표·분석 실 API 전환

고객 유입(§12)에 이어 매출 관리(D1~D5)를 DB 기반으로 전환. 계약·목표 데이터가 새로고침 후에도
유지되며, 달성률·ROAS/ROI·기여도는 서버가 계산한다.

### 백엔드 (`mcrm-v2-backend`)
- 스키마(설계 §3.5): `existing_customers`(유입 미연결 재계약·직거래, F-NTYUDC), `contracts`
  (new_lead/existing 분리, amount NULL=미입력 — 0으로 채우지 않음 §1.3.5), `contract_logs`·
  `goal_logs`(불변), `revenue_goals`(월×scope UNIQUE), `monthly_metrics`(과거 월 매출 스냅샷 +
  수기 광고비 — **B3 광고 API 커넥터 전 임시**).
- API: `GET/POST/PATCH /contracts`(직접 입력 시 유입 미연결 고객은 existing_customers 자동 등록,
  수정은 변경분만 불변 이력), `GET /customers/existing`+상세·자동완성(S-MYOVII)·중복 후보(S-MIQJYM),
  `GET/PUT /goals`(덮어쓰기+이력), `GET /revenue/analytics`(당월 실적 실시간 집계·scope별 추이·
  ROAS/ROI 이중축·채널 기여도).
- 계산 규칙: ROAS 분자=신규 유입 계약금액만, ROI 분자=전체 매출(S-BOZSZP·S-SVKWCD). 당월 광고비는
  경과일 비율로 페이스 환산(§7.3.1). 분모 결측 시 `null` 반환(0배 금지). 당월/경과일은 실제 시스템
  날짜 기준(목데이터의 고정 7/10 대체).
- 라우트 순서 주의: `/customers/existing*`을 `/customers/{customer}` 와일드카드보다 먼저 선언.

### 프론트 (`mcrm-v2`)
- **`lib/revenue-api.ts`** 신규 — mock-revenue와 동일 타입 재수출 + 계약/기존 고객/목표/분석 클라이언트.
- D1 계약 현황: 서버 목록(+임베디드 lead/existing 고객), 직접 입력 다이얼로그의 유입 고객 검색·기존
  고객 자동완성·중복 경고를 서버 호출(250ms 디바운스)로 교체. 이미 계약 있는 유입 고객은 덮어쓰기(PATCH).
- D2 금액 인라인 수정: PATCH + 실패 토스트. D3 목표: PUT 응답으로 그리드·이력 동기화.
- D4 분석: 전체 데이터 단일 fetch 후 scope 탭은 클라이언트 전환. D5 기존 고객 목록(계약 집계)·상세.
- 대시보드·보고서의 mock-revenue 사용부는 B3(광고 수집)·B4(보고서)에서 전환 예정 — 이번 범위 밖.

### 검증 (브라우저 실측)
- `tsc --noEmit` 통과, 콘솔 에러 0건.
- D1: 33건·완료 합계 ₩29.26M 렌더 → 직접 입력(기존·신규 등록 '노정민' ₩900,000, POST 201) →
  **하드 리로드 후 유지**(34건·₩30.16M, 연락처 서버 마스킹 010-****-7777).
- 중복 경고: 신규 등록 이름 '한지수' 입력 시 서버 중복 후보 안내 노출 확인.
- D3: 9월 목표 ₩47M 설정 → 리로드 후 그리드·불변 이력 유지. curl로 넣은 8월 ₩45M도 표시(영속 교차 확인).
- D4: 달성률 12일차/31일(실제 오늘), ROAS 0.59배·ROI 55.5%(7월 페이스 환산 광고비 ₩3.9M), 기여도
  구글/메타/기타 — 목데이터 규칙과 동일 판정(증거 없는 네이버 계약은 '기타').
- 한계: 광고비는 monthly_metrics 수기값(B3에서 수집 파이프라인으로 교체). 벌크 상태 변경·CSV 5,000건
  제한·역할별 내보내기 권한은 A1(인증) 이후. 재계산 큐(RecalculateMonth)는 미구현 — 현재는 조회 시 계산.

## 14. 2026-07-12 M3/B3 — 광고 수집 파이프라인 + 대시보드 완전 실데이터

광고 API 커넥터 아키텍처와 수집 파이프라인을 완성하고, 대시보드 3화면(통합 요약·채널별·캠페인별)과
상단 신뢰도 바를 전부 서버 데이터로 전환. **실 광고 API는 심사 대기 중이므로 목 커넥터**(v1 ADS_MOCK
패턴, 결정적 생성)로 파이프라인을 검증 — 심사 통과 시 커넥터 구현체만 교체하면 된다.

### 백엔드 (`mcrm-v2-backend`)
- 스키마(설계 §3.2): `ad_channels`(완결도·토큰 만료 D-5 시나리오), `ad_campaigns`(품질 지표 —
  귀속 엔진 고도화 전 임시), `ad_metrics_daily`(일별×캠페인×키워드, keyword='' 행이 캠페인 합계,
  UNIQUE로 소급 재수집 upsert), `collection_runs`(상태는 저장하지 않고 §1.1.2 규칙으로 파생).
- 파이프라인(§5): `CollectAdMetrics` 잡 — 06:00 KST 3채널 스케줄(routes/console.php), 실패 시
  백오프 5/15/30분×3회. 수동 재수집 최근 7일·하루 3회 제한(4회째 429). 메타 장애 시뮬레이션은
  **정기 수집에만** 적용되어 수동 재수집으로 복구되는 흐름을 재현(ADS_META_OUTAGE env).
- API: `/collection/status`(60초 폴링용)·`/collection/{code}/retry`, `/analytics/channels?period=m1|m3|m6`,
  `/analytics/campaigns`(키워드 드릴다운·매칭/귀속률), `/ad-channels/{code}/campaigns`(S-SSTEUI),
  `/dashboard`(KPI·목표·ROAS 추이·퍼널·채널 비교·비중·신뢰도 일괄 — 실패 채널은 합산 제외+basis 병기 §1.1.1).
- `RevenueMetrics` 공용 서비스 추출 — **매출 분석의 광고비를 수기값(monthly_metrics)에서 수집 지표
  합산으로 교체**(§13의 임시 광고비 해소). 당월은 실비용 기준이라 페이스 환산 제거. YoY 스냅샷(2025-07) 시드.
- 스케줄 실행: `php artisan schedule:work` + `php artisan queue:listen` (로컬).
- 트러블슈팅: Eloquent Collection `except()`는 컬렉션 키가 아닌 **모델 PK** 기준 — 집계(groupBy)
  행에서 전부 제거되는 버그. `->toBase()` 후 사용.

### 프론트 (`mcrm-v2`)
- **`lib/analytics-api.ts`** 신규. `data-trust.tsx` 재작성 — 상태 60초 폴링, retry가 실패 채널을
  자동 타깃, 성공 시 `version` 증가로 소비 화면들이 재조회.
- 통합 요약: 대시보드 컴포넌트 7종(KPI·목표 카드·ROAS 추이·퍼널·채널 비교·비중·신뢰도 요약)을
  하드코딩 시드 → props 기반으로 전환, 페이지가 `/dashboard` 단일 fetch로 공급. 퍼널은 누적 도달
  고객 수 + 최저 전환 구간 하이라이트(전월 대비 증감은 이력 집계 도입 후).
- 상단 신뢰도 바·실패 배너: 채널별 ✓/⚠/✕ 실상태, 배너에 잔여 재시도 횟수 표기.
- 채널별/캠페인별 상세: 기간별(m1/m3/m6) 서버 집계 렌더. C3 수기 입력 캠페인 드롭다운은 채널 선택
  시 서버 조회(마지막 수집 시각 라벨 포함).

### 검증 (브라우저 실측)
- `tsc --noEmit` 통과.
- 초기 상태: 신뢰도 바 "네이버 ✓ · 메타 ✕ 수집 실패 · 구글 ✓", 배너 "마지막 수집 3일 전 ·
  오늘 3회 남음", KPI 광고비 ₩2.2M(수집 성공 2/3 기준)·전환 30건.
- **재시도 클릭 → 메타 복구**: 신뢰도 바 3채널 ✓ "0초 전", 배너 소멸, KPI 자동 재조회
  (광고비 ₩3.5M·3/3, 전환 54건, ROAS 0.73→0.66배 — 메타 포함으로 분모 증가). curl로 4회째 429 확인.
- 채널별 상세(최근 3개월): 합계 ₩23.1M·ROAS 2.89배, 채널별 CTR/CPC/완결도, 월간 스택 막대.
  캠페인별: ROAS 내림차순 7개 캠페인 + 키워드 수·매칭/귀속률 경고색.
- 한계: 목 커넥터(실 API 심사 필요 — 네이버/메타/구글 선신청 권장). 캠페인 품질 지표(매칭/귀속률)와
  채널 완결도는 시드값(귀속 엔진 v2에서 실계산). 퍼널 전월 대비 증감·주간 뷰 미구현. 메타 복구 후
  초기 실패 상태 재현은 `php artisan migrate:fresh --seed`.

## 15. 2026-07-12 네이버 검색광고 실 API 커넥터 (§14 후속)

네이버 검색광고는 심사 없이 API 키를 즉시 발급받을 수 있고 v1(mcrm-backend)에 검증된 연동
(`NaverAdsApiService` + 운영 자격증명)이 이미 있어, 실 커넥터를 바로 이식했다.

- **`NaverAdsConnector`** — v1에서 실계정으로 확인된 제약까지 그대로 이식: /stats의
  timeIncrement(일별 분할)는 11001로 거부 → since=until 캠페인별 개별 호출, 계정당 100 req/s
  제한(429 실측) → Http::pool 청크 20 + 0.3s 간격 + 실패분 1회 재시도. 캠페인 목록 1시간 캐시.
- **`ChannelRoutingConnector`** — 채널별 라우팅: 네이버는 `ADS_NAVER_REAL=true`+자격증명 시 실 API,
  메타/구글은 심사 통과 전까지 목. 자격증명은 v1 .env에서 이관(NAVER_ADS_*, gitignore).
- **점검·전환 커맨드**: `php artisan ads:test-naver`(조회 전용 점검), `php artisan ads:naver-real
  --days=30`(네이버 목 지표 삭제 후 실 데이터 소급 수집 — 목/실 캠페인 혼입 방지).
- **실계정 검증 완료**: 캠페인 11개(인사이트/파워링크#1~6/파워컨텐츠#1/플레이스 등), 최근 3일 일별
  지표(예: 7/10 인사이트 노출 2,090·클릭 2·₩23,167) 정상 조회.
- 주의: 실 캠페인에는 CRM 계약 매칭이 아직 없어 전환 시 네이버 ROAS 분자(확정 귀속 매출)가 0으로
  표시됨 — 귀속 엔진(고객 campaign_name ↔ 실 캠페인 매칭) 도입 후 해소. revenue_attributed를
  플랫폼 전환값으로 채우지 않는 것은 의도(§1.3.4 증거 기반 귀속 원칙).
- 기본값은 `ADS_NAVER_REAL=false`(데모 목 데이터 유지). 실 전환은 .env 변경 + `ads:naver-real` 실행.

## 16. 2026-07-12 M3/B4 — 보고서 실 API + 개인정보 배치 + 온보딩 API

백엔드 마지막 마일스톤(B4). 보고서 E1~E4가 DB 기반으로 전환되어 생성 버전이 영구 보존되고,
개인정보 익명화 배치와 온보딩 체크리스트 API가 추가됐다.

### 백엔드 (`mcrm-v2-backend`)
- **보고서(§3.6)**: `reports`(생성 시점 실 집계 스냅샷 — 기간×채널×캠페인 광고비, 확정 귀속/전체
  매출, 입력률. 분모 결측 시 roas/roi null), `report_exports`(불변 90일), `saved_filters`(사용자당
  10개·이름 중복 차단). 동기 생성 — 배포 시 GenerateReport 큐 이관 예정.
- **내보내기**: excel=CSV(BOM, 한글 엑셀 호환) 스트림 + 이력 기록. pdf=이력만 기록하고 클라이언트
  인쇄(print) — Browsershot/chromium 도입 전 실용 대안.
- **개인정보(§2.1.6)**: `AnonymizeExpired` 잡(매일 03:00) — `privacy_retention_months`(기본 12,
  system_settings) 만료 고객·기존 고객의 직접 식별자(이름/연락처/메모) 익명화 + `privacy_logs`(불변).
  수동 실행 `php artisan privacy:anonymize`. F3 화면용 `GET /privacy/summary`·`PUT /privacy/retention`.
- **온보딩(§6.3)**: `GET /onboarding/checklist` — 채널 연결/고객 데이터/당월 목표를 실상태에서 파생,
  전체/부분/비활성 판정.
- `ReportSeeder` — mock-reports 버전 5개(실패 1건 포함)·저장 필터 2개 이식. 이미 데이터가 있으면
  스킵(네이버 실데이터 보존), `php artisan db:seed --class=ReportSeeder` 단독 실행 가능.

### 프론트 (`mcrm-v2`)
- **`lib/reports-api.ts`** 신규. E1 목록(실패 제외·배지), E2 생성(저장 필터·캠페인 풀 서버화 —
  실 네이버 캠페인 포함), E3 뷰어(summary는 스냅샷 고정, 차트 섹션은 최신 수집 기준 — 퍼널 하드코딩
  342건 제거하고 대시보드 실데이터), E4 비교 전환. Excel 다운로드·PDF 인쇄 모두 서버 이력 기록.

### 검증 (브라우저 실측)
- `tsc --noEmit` 통과, 콘솔 에러 0건.
- E1: 시드 5건 + curl 생성 v7 표시, 실패 v3 제외, v6 "데이터 업데이트됨" 배지.
- E2: UI 생성 → v8 (7월 전체 — 광고비 ₩1.96M은 네이버 실데이터+메타/구글 목 합산, ROAS 0.41배,
  ROI 215%, 입력률 71%) → 뷰어 렌더 → **하드 리로드 후 유지**(§10 한계 해소).
- E3: 퍼널 실데이터(47→9건, 19.1%), 캠페인 ROAS에 실 캠페인 '인사이트' 표시. E4: v7↔v8 동일 조건
  "변동 없음" 정확 표시. CSV 내보내기·PDF 이력은 curl로 확인(report_exports 2건).
- 한계: 보고서 차트 섹션은 스냅샷이 아닌 최신 수집 기준(스냅샷 확장은 후속). PDF는 브라우저 인쇄
  (서버 렌더링은 chromium 도입 시). 역할별 마스킹·내보내기 권한은 A1(인증) 이후. F3/온보딩 화면의
  프론트 연동(신규 API 사용)은 후속.

## 17. 2026-07-13 광고 퍼널 분리 + 전사 지표 수익률 전환 (운영 커스터마이징)

> 배경: 이 시스템은 인사이트의 **매출 보고**가 1차 목적. 광고비가 매출의 ~3%라 구
> ROI((전체 매출 − 광고비) ÷ 광고비)는 전사 지표로 부적합하고, 전체 문의 퍼널은
> 유입 이벤트=리드가 항상 동시 통과라 첫 구간이 무의미했다 (사용자 확정 3건:
> ① 퍼널은 온라인 광고 기준으로만 ② 위치는 유입 관리 ③ ROI → 수익률(수익÷매출)).

### 백엔드 (`mcrm-v2-backend`)
- **`GET /analytics/ad-funnel?from&to`** 신설 (DashboardController::adFunnel) —
  집계 대상: `source_category=online_ad` (귀속 확정 + 소스 미상(온라인 추정) 포함, 병기 카운트 반환).
  단계: 온라인 문의 → 상담 → 견적/제안 → 계약 (누적 도달, 리드는 문의와 동시 통과라 생략).
  클릭은 월 단위 집계 — 기간은 겹치는 월 합산 근사. 기간 지정 시 전년 동일 기간(prevYear) 동봉.
- **`RevenueMetrics::roasRoiTrend`**: `roi` → `margin`(수익률 % = (매출 − total_cost) ÷ 매출,
  당월은 currentProfit ÷ currentRevenue). 주간 보고서의 '수익률'과 동일 정의 — 7월 82.8%로 실측 일치.
- 대시보드 kpi: `roi/roiMoM` → `margin/marginMoM`(%p). 퍼널 기간 필터·funnelPrevYear는
  대시보드에서 제거(광고 퍼널로 이동), funnel(전체 파이프라인)은 KPI 카드용으로 유지.

### 프론트 (`mcrm-v2`)
- **`/customers/ad-funnel`** 신설 + 사이드바 '광고 퍼널' 메뉴 — 클릭→문의 밴드(스케일이 달라
  깔때기와 분리 표기), 4단 퍼널, 기간 프리셋(대시보드에서 이사), 전년 동일 기간 줄, 포함 기준 캡션.
- 대시보드: 전환 퍼널 카드·기간 셀렉트 제거(Top 거래처 전폭), KPI ① 수익률 병기,
  광고 성과 섹션에 퍼널 위치 안내.
- 추이·기여도 이중축 차트: ROI → **수익률(수익÷매출)** (§7.3.1 개정), 툴팁·경고 문구 갱신.
- 삭제: funnel-chart.tsx·kpi-cards.tsx (미사용), mock-revenue `RoasRoiPoint.roi`→`margin`.

### 검증
- tsc 통과·콘솔 에러 0. 광고 퍼널: 클릭 40,337 → 문의 4(확정 1·추정 3, 0.01%) → 상담 4 → 견적 2 → 계약 0.
- 이중축 차트 7월 수익률 82.8% = 주간 보고서 82.76%와 일치. KPI 수익률 82.8% 표시.
- 한계: 광고 문의 모수가 아직 작음(UTM 태깅 정착 전) — 증거가 쌓이면 확정 귀속 기준으로 좁힘.

## 18. 2026-07-20 전 화면 기간 필터 + 대시보드 월 되감기

> 사용자 요청: "광고 퍼널처럼 모든 페이지에 기간 설정" — 화면 성격별로 형태를 나눠 전부 적용.

- **공용 `components/shared/period-select.tsx`** (광고 퍼널에서 추출): 프리셋(전체/이번 달/30일/3개월/올해/직접 입력) + `usePeriod` 훅. 광고 퍼널도 이걸로 리팩터링.
- C1 유입 목록(유입일 — 단계 요약 칩도 기간 반영) · D1 계약 현황(계약일) · B3 캠페인(기본 이번 달) 기간 셀렉트, B2 채널 탭에 '직접 입력', 추이·기여도 시작월~끝월(작년 포함 시 라벨 연도 병기 '25.11월').
- **대시보드 월 되감기**: 최근 12개월 셀렉트 — 달성률·KPI·신규/기존 구성·채널 비교·비중이 해당 월 기준. 완결월은 경과 100%·예측=실적·YoY 페이스 1 ("완결월 조회 중" 배지).
- 백엔드: 전 API from/to·month 파라미터 (fc83e08), 광고 지표는 월 단위라 겹치는 월 합산 근사.
- 검증: 6개 엔드포인트 스모크(6월 문의 10건·계약 23건·6월 되감기 61.9% 등) + tsc + 운영 배포 후 외부 HTTPS 재검증 완료.

### §18 후속 폴리싱 (같은 날, 사용자 피드백 3건)

- **위치 통일**(b64f4e6): 기간 UI를 전 화면 우측 상단(제목 행)으로 — 유일하게 별도 줄이던 B2 이동.
- **B2도 공용 셀렉트로**(769bdf7): 월 단위 탭(전월/3개월/6개월) 제거 → PeriodSelect(기본 최근 3개월).
  '전체 기간'은 광고 데이터 시작(2025-01)부터로 명시 매핑 — 기간 미지정=당월 폴백 함정 제거.
  보고서 뷰어(E3) 차트 섹션은 `presetRange('month')`로 당월 유지.
- **프리셋 개편**: '최근 30일' 제거 → **'지난 달'** 추가 (주간 보고+월 마감 리듬 기준, 사용자 확인).
  최종 프리셋: 전체 기간 / 이번 달 / 지난 달 / 최근 3개월 / 올해 / 직접 입력.
- **메타·구글 7월 수치 출처 검증**(사용자 문의): 목 아님 — 광고성과 시트 7월 1주차 실측
  (메타 ₩77,711·GDN ₩17,492·유튜브 ₩13,410, 운영 DB↔CSV 원 단위 일치, 목 캠페인 0건).
  시트 2주차 이후 기입 시 `--ads-only`로 갱신.

## 19. 2026-07-20 A1 로그인 — 관리자 등록 방식 (Sanctum 토큰)

- **인증**: POST /auth/login(유일한 공개 라우트) → Sanctum 개인 액세스 토큰(localStorage).
  전 API auth:sanctum 보호(미인증 401 → 프론트 공통 처리로 /login 리다이렉트).
  이력 행위자 고정(시드 팀장) 제거 — 감사 로그가 실제 로그인 사용자로 기록.
- **계정 체계**: 공개 가입 없음 — F2(설정›사용자 권한 관리)에서 팀장·관리자가 등록·비밀번호 초기화.
  초기 비밀번호는 첫 로그인 시 변경 강제(password_changed_at null), 변경·초기화 시 타 기기 토큰 폐기.
- **화면**: /login 신설(로그인+비번 변경 단계), AppShell 인증 가드·실사용자 표시·로그아웃,
  F2 실계정 테이블(마지막 로그인·비밀번호 상태)+등록 다이얼로그.
- **배포 사고 2건**(수정 커밋 참조): ① last_login_at 중복 ALTER(create_users에 이미 존재) →
  마이그레이션 기록 전 실패로 앱 크래시루프 ② personal_access_tokens.tokenable_id BIGINT ↔ UUID PK
  불일치 → MySQL만 로그인 500. 교훈: SQLite는 타입 관대 — 스키마 변경은 hasColumn 가드 + MySQL 검증.
- **Caddy 공용 basic_auth 제거** — 개인 로그인이 잠금장치. 운영 5계정 초기 비번 발급(첫 로그인 시 변경).

## 20. 2026-07-20 광고 성과 월별 수기 입력 (API 미연동 채널)

- 사용자 지적: 메타·구글은 API가 없으니 채널 성과도 화면에서 수기 입력할 수 있어야 한다(시트 CSV 교체는 개발자용 절차).
- **GET/POST /ad-metrics/manual** (AdMetricManualController): 메타·구글 월별 노출·클릭·광고비·전환수 입력.
  저장은 (채널,월) authoritative — 해당 채널·월 기존 행 전량 삭제 후 수기값 1행 대체(시트 임포트와 이중 계상 방지).
  네이버는 실 API 소스라 422 차단, 미인증 401.
- **설정 › 광고 채널 연동** 하단에 입력 폼 + 이력 표. 대시보드·채널별 상세(월 단위 집계)에 즉시 반영.
- 운영 원칙: 메타·구글은 **시트 CSV 또는 화면 입력 중 한 방법만** (섞으면 대체 규칙상 마지막 것만 남음). 갱신 가이드 B절에 명시.
- 검증: 로컬 입력→집계 반영(메타 ₩900k·구글 ₩250k)·네이버 422, 운영 외부 HTTPS 401/422 확인.
- **CSV 벌크 업로드 추가**(583ca20): POST /ad-metrics/manual/batch — 여러 (채널,월) 행 일괄, 각 행 authoritative 대체. 설정 화면에 파일 선택(클라 CSV 파싱)+서식 CSV 내려받기. 헤더 채널,월,노출,클릭,광고비,전환수(채널 메타/구글, 월 YYYY-MM). 광고성과 구글시트 원본이 아닌 간단 6열 서식 — 시트 원본 파서는 필요 시 후속.

## 21. 2026-07-21~22 운영 피드백 4건 (보고서·매출·표·유입일)

사용자 운영 중 발견/요청 4건. 모두 로컬 검증 → 커밋 → VPS 재빌드·배포 완료.

- **주간 보고서 매출/ROAS ₩0 버그 수정 (A+B, 백 ccf042f / 프론트 bf3fcce)**: 매출 자료가 월 단위(계약일 월 중순 귀속)라 주간으로 자르면 매출·ROAS·ROI가 0이 되던 문제. **A** — 주간 보고서는 매출·ROAS·ROI를 **해당 월 기준**으로 계산하고 `summary.revenueBasisLabel`('○월 기준 · 월 단위 자료')로 명시(광고비는 주간 그대로). **B** — 퍼널·채널을 발행 시점에 **보고서 기간으로 집계**해 `data_snapshot`에 고정(ReportService::periodFunnel/periodChannels), 뷰어가 실시간 대신 스냅샷 사용(구 보고서는 없어 실시간 폴백). 주의: Report::toFrontend의 `channels`는 필터 라벨용이라 채널 지표는 `channelRows` 키로 분리 노출. **기존 프로덕션 주간 보고서는 재생성해야 교정치 반영**(summary는 늘 발행 시점 고정).
- **매출 예상/실매출 분리 + 계약 상태 변경 UI (프론트 b0e5575)**: 계약 현황 요약을 **실매출(완료)·예상 매출(진행 중)** 2개 카드로 분리(취소 제외). 금액은 1칸 유지, 상태로 예상/실 분류. 편집 다이얼로그에 **상태 변경(진행 중/완료/취소)** 추가 — 백엔드 PATCH /contracts는 원래 status 변경·이력 지원했으나 화면엔 없던 기능. 상태명은 그대로 유지(완료=done가 ROAS·수익률·목표달성·보고서 집계 전반에 연동).
- **표 열 너비 고정 (프론트 a2375cb)**: `table-auto`(내용 기반) → `table-fixed` + 열별 고정 너비. 기간/필터로 행이 바뀌어도 열 폭이 데이터와 무관하게 일정. 계약 현황·고객 목록은 %폭, 채널별·캠페인별 상세는 px폭+min-width(가로 스크롤).
- **유입일 지정 3경로 (백 783ecb3·26745b0 / 프론트 d31ffb9·21eabb4·861ff3d)**: 유입 고객 유입일이 입력일(now())로 고정되던 것을 변경 가능하게. ① **수기 입력**: 유입일 date 필드(기본 오늘·max=오늘) 추가. ② **상세 편집**: 기본 정보 편집에 유입일 필드 추가, 변경 시 이력 기록. ③ **파일 업로드**: 발생일 컬럼을 파싱만 하고 저장 안 하던 버그 수정(payload에 inflowDate 추가). 백엔드 store/update가 inflowDate(nullable/date) 수용, `inflow_date`와 입력률 KPI용 `InflowEvent.occurred_at`을 함께 설정. 실데이터 점검: 오늘 잘못 저장된 고객 0건(웹 생성 4건은 실제 유입일 07-20 확인).

## 22. 2026-07-28 매출 개편 — 계약금액/매출금액 분리·분납·프로젝트 묶기·비용 입력

배경: 계약 총 1,000만원을 300/400/300으로 나눠 수금하는 분납, 같은 프로젝트 매출 묶어 보기, 건별 지출로 순매출 계산 요구. 아키텍처: **contracts는 "월별 매출 인식 행" 유지 + 상위 `contract_groups`(프로젝트) 계층 추가** — 집계 4계층 무변경, 계약금액(total_amount)은 표시 전용(집계는 오직 회차 amount 합, 이중 계상 금지).

- **1단계 — 비용 개통 + 스냅숏 버그 3건 (백 43795af / 프론트 afbed56)**:
  - store/update에 `cost` 검증·저장('비용' 이력), toFrontend 노출. 등록·편집 다이얼로그 비용 입력, 표 매출금액 셀 `비용 X · 순 Y` 서브라인, 실매출 카드 `지출 Σ · 순매출 Σ`, CSV 열 추가.
  - **MonthlySnapshots 서비스**(단일 정본): ① ImportRealData가 total_cost 누락(수익이 비용 0으로 계산) ② 과거 월 수기 편집 시 스냅숏 정체(임포터 재실행 때만 갱신) ③ ImportClientRevenue placeholder 삭제에 external_source=file 가드 없음(수기 미입력 행이 재적재 때 삭제될 위험) — 3건 동시 수정. 컨트롤러가 과거 월 변경 시 해당 월만 즉시 rebuild(계약일 이동 시 양쪽 달).
- **2단계 — 프로젝트·분납 (백 27de711 / 프론트 7542054)**:
  - `contract_groups`(customer 연결·name·total_amount·memo) + `contracts.group_id`. POST /contracts `installments[]`(계약일·금액·비용) 단일 트랜잭션 다중 등록 + totalAmount/groupName/**groupId(기존 프로젝트에 회차 추가)**. 구 단건 형태는 단일 객체 응답 유지(하위 호환). PATCH /contracts `groupId`(나중에 묶기, '프로젝트' 이력), PATCH /contract-groups(총액·이름·메모).
  - 계약 현황 **8열**: 고객명(+프로젝트 뱃지) | 유형 | 유입경로 | 담당자 | 계약일 | **계약금액**(총액+수금 진행률 `수금 ₩X · N% · n회`) | **매출금액**(회차 수금+순매출) | 상태.
  - 입력 다이얼로그: 회차 여러 줄(+ 회차 추가), 프로젝트 섹션(총액·프로젝트명·기존 프로젝트 선택), 합계≠총액 경고(차단 없음). **1고객 1계약 덮어쓰기 제거** — 항상 새 회차 추가, placeholder 흡수는 서버 이관(file 출처만).
  - 편집: 프로젝트 컨텍스트 + 지정/해제. 기존 고객 상세: 프로젝트 뱃지·순매출·'누적 매출' 라벨.
- 검증: 분납 3회(300/400/300·총 1000만) 등록 → 회차 3행·그룹·과거월 스냅숏(5·6월) 반영·진행률 100%, 구 스칼라 POST 하위 호환, import:revenue 재실행 합계 불변(₩1,260,131,354)·수기 그룹 행 보존, MySQL 마이그레이션 정상(844ms). 3단계(파서 카테고리 자동 추출·보고서 수익 표기)는 태스크 #42로 보류.
- **후속 폴리싱(c452688)**: 요약 카드를 순매출 중심으로 — 메인 숫자 = 순매출, 하단 `매출 X − 지출 Y` 병기('실매출' 표기는 매출/순매출로 정리).
- **추이·기여도 분석 순매출 전환(백 4c8c5f3 / 프론트 909c567)**: ① 매출 추이 → **순매출·매출 추이**(전체 탭: 순매출 주선+매출 보조선+목표(순매출), 호버 툴팁 3종 — 신규/기존 탭은 유형별 비용 스냅숏이 없어 매출 기준 유지) ② 채널 기여도 → **순매출(매출−비용) 기준**(백엔드 net 필드 추가·정렬 net, 범례에 매출 병기, 구 응답은 매출 폴백) ③ 용어 '수익'→'순매출'(달성률 배지·수익률 정의=순매출÷매출) ④ **대시보드도 통일(173782e)**: 목표 달성률·이번 달 순매출 KPI·순매출·매출 추이(범례·툴팁·전년·목표) — 계산 로직 변경 없음. ⑤ **보고서 순매출(백 c0400f8 / 프론트 b8fbe77)**: 스냅샷 summary에 cost·netRevenue(매출과 동일 창·스코프) 추가, 뷰어 주간/월간/연간 카드가 순매출(매출−지출 병기)로 — 구 보고서는 필드 없음 → 기존 전체 매출 표기 폴백(섹션 제목 포함). ⑥ **기간 프리셋·표기 정비(0c10034)**: '이번 달/올해/최근 3개월' 끝값이 오늘까지로 잘려 미래 날짜 계약(분납 회차·월말 귀속)이 누락되고 대시보드(월 전체)와 어긋나던 버그 → 끝값을 월말/연말로. 기간 필터 기본값 전 화면 '이번 달' 통일(계약현황·고객목록·광고퍼널·채널상세). 금액 표기 원칙: 반올림(₩x.xM)은 그래프 내부 라벨만, 카드·표·범례·텍스트 패널은 원 단위.

## 2026-07-29 버그픽스 — 매출 직접 입력 다이얼로그 (병원명 체계 정합)

사용자 리포트 3건 검토 결과, 7/20 병원명(hospital_name) 체계 전환이 다이얼로그·검색에 미반영된 것이 원인.

1. **신규 유입 검색 미작동** — 서버 `q` 검색이 빈 `name` 컬럼만 LIKE(실데이터 식별자는
   `hospital_name`) → 병원명 검색 항상 0건. 서버 검색에 hospital_name 추가로 해소.
2. **탭 타이틀이 바뀐 느낌** — 타이틀 자체는 정상. 신규 유입 탭만 병원명 검색이 안 되고 결과도
   빈 name을 표시해 기존 고객 탭과 뒤바뀐 것처럼 보였던 것. 결과·선택 표시를 hospitalName 중심으로
   변경(담당자명은 보조 표기), placeholder도 '병원(업체)명 · 담당자명 · 연락처'로 정정.
3. **기존 고객 신규 등록 미반영** — 저장 로직 자체는 정상(재현: POST 201 → 리스트 즉시 반영 확인).
   검증 실패(담당자 미선택 등) 시 다이얼로그 하단 문구만 떠서 조용히 무시된 것이 원인으로 추정 —
   toast.error 병행으로 실패를 명확히 알리도록 수정.
- 부수 수정: 계약 현황 리스트의 신규 유입 고객명 '미기재' 표시 → hospitalName 우선(customerLabel).
- 검증: '치과' 검색 → 병원명 결과 5건(이앤미치과병원 등) 표시, 신규 유입 계약 행 '김해복음병원' 표기
  확인. 재현용 테스트 데이터(버그검증의원)는 DB에서 정리 완료.

## 23. 2026-07-29 유입 고객 삭제 + 휴지통 (30일 보관 → 백업 후 영구삭제)

- **삭제(백 ede8d68 / 프론트 bc660b0)**: 유입 고객 목록 행에 휴지통 이동 버튼 — 권한은 **해당 고객의 담당자 또는 관리자**(버튼 노출 + 서버 검증, 위반 시 403). 매출 계약이 연결된 고객은 삭제 차단(422 — 매출 관리에서 먼저 정리). 삭제 시 '삭제' 이력 기록, soft delete라 **목록·퍼널·집계 전 쿼리에서 즉시 제외**.
- **휴지통 페이지**(/customers/trash, 사이드바 링크): 삭제일·삭제자·영구삭제 예정(D-n, 7일 이하 경고색). **복원**(담당자·관리자, '복원' 이력) / **즉시 영구삭제**(관리자만).
- **자동 영구삭제**: `customers:purge-trash` 매일 03:30 — 삭제 30일 경과 고객을 **일자별 JSON 백업**(storage/app/private/trash-backups/YYYY-MM-DD.json, 연락처 복호화 포함 — 서버 로컬 한정) 후 관련 데이터(귀속 증거·이력·유입이벤트 매칭)까지 영구삭제. 계약 연결 발견 시 안전상 건너뜀.
- 검증: 삭제→휴지통(D-30)→목록 제외→복원 왕복, staff의 타인 고객 삭제 403, 계약 고객 422, 31일 프로브 퍼지→백업 파일 생성, 프로덕션 MySQL 마이그레이션·스케줄 등록 확인.

## 24. 2026-07-29 계약 삭제 + 계약 휴지통 (§23과 동일 체계)

- **삭제(백 4582ccd / 프론트 5347fcd)**: 계약 현황 행에 휴지통 이동 버튼 — 권한은 **해당 계약 고객의 담당자 또는 관리자**(버튼 노출 + 서버 검증, 위반 시 403). 삭제 시 '삭제' 이력 기록, soft delete라 **순매출·예상 매출·추이·대시보드 등 전 집계에서 즉시 제외**되고, 과거 월 계약이면 해당 월 monthly_metrics 스냅샷을 즉시 재계산.
- **계약 휴지통 페이지**(/revenue/trash, 매출 관리 사이드바 링크): 고객명·유형·매출금액(비용)·계약일·상태·삭제일·삭제자·영구삭제 예정(D-n, 7일 이하 경고색). **복원**(담당자·관리자, '복원' 이력 + 해당 월 스냅샷 재계산) / **즉시 영구삭제**(관리자만).
- **자동 영구삭제**: `contracts:purge-trash` 매일 03:35 — 삭제 30일 경과 계약을 **일자별 JSON 백업**(trash-backups/contracts-YYYY-MM-DD.json, 고객·담당자·프로젝트·금액·비용·이력 포함) 후 이력과 함께 영구삭제, 과거 월이면 스냅샷 재계산.
- **임포터 안전장치**: `import:revenue`의 file 소스 행 재적재 삭제와 placeholder 흡수는 forceDelete로 전환 — 시스템 삭제 수백 건이 휴지통에 쌓이지 않음.
- 검증: 삭제→휴지통(D-30)→목록·순매출 즉시 제외(₩34,385,924→₩33,785,924)→복원 왕복, 6월 계약 삭제·복원 시 스냅샷 정확 재계산(20,645,201↔20,016,500), staff의 타인 계약 삭제 403, staff 영구삭제 403·버튼 비노출, 퍼지→contracts-백업 파일 생성, `import:revenue` 재실행 후 합계 불변·휴지통 오염 없음, 프로덕션 MySQL 마이그레이션(441ms DONE)·03:35 스케줄 등록 확인.

## 25. 2026-07-29 계약 수정 팝업 확장 — 매출 입력과 동일 항목 (백 78731d9 / 프론트 5690343)

- **계약일 편집**: 수정 팝업(EditAmountDialog)에 계약일 date 입력 추가 — 이동 시 이전·이후 달 monthly_metrics 스냅샷 모두 재계산(백엔드 기존 지원 활용), 변경 이력 '계약일' 기록.
- **프로젝트 섹션**(매출 입력과 동일한 박스 UI): ① **+ 새 프로젝트 만들기** — 프로젝트명·계약금액(총액) 입력 → `PATCH /contracts/{id}`에 신설된 `groupName`·`groupTotalAmount`로 그룹 생성 후 소속(이력 '프로젝트'), ② **기존 프로젝트 이동/해제**(기존 groupId 유지), ③ **선택된 프로젝트의 이름·계약금액(총액) 수정** — 값이 바뀐 경우에만 `PATCH /contract-groups/{id}` 추가 호출. 선택 변경 시 해당 프로젝트 값 프리필.
- 라벨 '계약 금액' → **'매출금액'** 으로 입력 다이얼로그와 용어 통일. 상태·비용 편집은 기존 유지.
- 검증(로컬): PATCH groupName+총액 → 그룹 생성·소속·이력, 계약일 07-15→07-16 이동·이력, contract-groups 메타 수정, 팝업 UI로 새 프로젝트 생성(리뉴얼검증·₩1,000,000)→재오픈 시 프리필 확인→이름·총액 수정(리뉴얼검증2·₩2,000,000) 반영. 테스트 데이터 원복 완료. 프로덕션 배포·스모크(200/401) 통과.

## 26. 2026-07-29 기존 고객명 수정 + 동일 이름 무손실 병합 (백 b02618c / 프론트 b2d9c79)

- **고객명 수정**: 기존 고객 목록의 고객명 셀에 연필 버튼(호버 노출) → 수정 다이얼로그. `PATCH /customers/existing/{id}` 신설.
- **동일 이름 병합**: 입력 중 동일 이름 고객을 실시간 감지(250ms 디바운스)해 경고 배너 + '병합하며 저장' 버튼으로 전환. 저장 시 서버가 병합(ExistingCustomerMerge):
  - 계약(**휴지통 행 포함**)·프로젝트(contract_groups) 전부 이름 보유 고객으로 이관 — 집계·이력 무손실.
  - 빈 필드 보완(연락처·담당자·최초 계약일은 더 이른 값), 메모 이어붙임 + `[병합 날짜] '이름' 고객 흡수 — 계약 n건 이관` 주석.
  - 흡수된 원본 행은 일자별 JSON 백업(`trash-backups/existing-merged-YYYY-MM-DD.json`, 연락처 복호화 포함 — 서버 로컬 한정) 후 제거.
  - 주의: `merged_from_customer_id` 컬럼은 유입 고객(customers) FK라 기존↔기존 병합에는 쓰지 않음 — 출처는 메모·백업이 정본.
- 현재 데이터 점검: 로컬·프로덕션 모두 중복 고객명 0건 (사전 정리 불필요, 향후 발생분만 병합 대상).
- 검증: 단순 이름 변경, A(계약 1건+휴지통 1건+프로젝트)→B 병합 — B 계약 3건(휴지통 포함)·프로젝트·합계 ₩444,000 정확, 백업 파일 생성, UI 다이얼로그 프리필·중복 감지·버튼 전환 확인. 테스트 데이터 원복. 프로덕션 배포·스모크(200/401) 통과.

## 27. 2026-07-29 계약 현황 표 — 병원명·고객명(담당자) 열 정렬 (프론트 e5fdb5e)

- 유입 고객 목록과 표기 통일: '고객명' 열 → **'병원명'**, 그 옆에 **'고객명(담당자)'** 열 추가.
- 고객명(담당자) 값: 신규 유입 = 연결된 유입 고객의 담당자명(미입력 시 —), 기존 고객 = —(업체명 단일 식별이라 담당자 개념 없음).
- 검색에 담당자명 포함(placeholder '병원명 · 고객명 · 담당자'), CSV 내보내기 열 동기화, 10열 너비 재배분(table-fixed 유지).
- 참고: 현재 실데이터의 유입 고객 122건 모두 담당자명 미입력이라 당장은 — 표시 — 수기 입력·편집으로 이름이 채워지면 자동 표기(임시 데이터로 표시 검증 완료).

## 28. 2026-07-29 기존 고객 목록도 병원명·고객명(담당자) 정렬 (백 5629c99 / 프론트 4c36536)

- 기존 고객 목록 표도 동일 정렬: '고객명' 열 → **'병원명'**, 옆에 **'고객명(담당자)'** 열 추가.
- 기존 고객에는 담당자 필드가 없었으므로 **`existing_customers.contact_name`(nullable) 신설** — 열이 항상 비지 않도록 §26 수정 다이얼로그에서 병원(업체)명과 함께 담당자명 입력 가능(PATCH contactName).
- 병합 시 남는 고객의 빈 담당자명은 흡수되는 고객 값으로 보완, 병합 백업 JSON에도 포함. 검색에 담당자명 포함.
- 검증: 표 헤더 7열 렌더, 다이얼로그에서 담당자명 입력→표 즉시 반영→DB 저장 확인 후 원복. 프로덕션 MySQL 마이그레이션(169ms DONE)·스모크(200/401) 통과.

## 29. 2026-07-29 표 정렬 규칙 — 병원명 중앙·매출금액 좌측·프로젝트명은 금액 상단 (프론트 79d17b9)

- 전 표 공통 규칙: **병원명 중앙정렬**(기본값 유지 — 기존 고객 목록 연필 래퍼가 좌측으로 틀어놨던 것 justify-center로 교정), **매출금액 헤더·값 좌측정렬**.
- **프로젝트명 위치 변경**: 병원명 옆 배지 → **계약금액 셀 상단** 표기(계약 현황). 계약금액 없는 표(계약 휴지통·기존 고객 상세)는 매출금액 상단에 표기.
- 기존 고객 상세 이력 표의 '계약 금액' 라벨을 '매출금액'으로 통일.
- 적용 표: 계약 현황, 계약 휴지통, 기존 고객 목록, 기존 고객 상세. 검증: computed style로 병원명 center·매출금액 left, 프로젝트명이 계약금액 셀 상단(₩1,200,000 + 수금 진행률 위)에 표기됨 확인(임시 프로젝트로 검증 후 원복).
- 후속(793a836): **계약금액도 좌측정렬**. 계약 현황 매출금액 셀은 **순매출(제일 크게) → `매출 X · 지출 Y`** 위계로 변경 — 요약 카드와 동일한 순매출 중심 표기(비용 미입력 시 순매출=매출 한 줄만).
- 후속(801894e): 계약 현황 **예상 매출 집계 기준을 '완료+진행 중'으로 확장** (취소만 제외) — 카드 배지·하단 안내문 동기화.

## 30. 2026-07-31 메타 광고 실 API 연동 완료 (백 ed87b1a)

- **MetaAdsConnector** 신설 — Graph API Marketing Insights(`act_{id}/insights?level=campaign`)로 일별 캠페인 지출·노출·클릭·lead 전환 수집. ChannelRoutingConnector에 meta 라우팅(`ADS_META_REAL=true` + 토큰 있을 때만 실 커넥터).
- **인증**: 개발 모드 앱(mcrm-insights) + 비즈니스 **시스템 사용자 토큰(만료 없음, `ads_read`만, 광고 계정 '성과 보기' 권한만)**. 토큰·계정 ID는 서버 .env 전용(git 제외). 비즈니스 미인증이어도 자사 광고 계정 조회 가능.
- **데이터 정리**: 7월 API 백필(₩668,241, 61행) 후 시트 월합계 행('메타 유료광고' 2026-07, ₩77,711) 삭제 — 이중 계상 방지. 2025-08~2026-06 시트 월합계 행은 해당 월의 유일한 소스라 유지.
- 이로써 **네이버·구글·메타 3채널 모두 매일 06:00 실 API 자동 수집** 체제 완성. 검증: 커넥터 라우팅·오류 경로(가짜 토큰 code 190 진단), 전일 수집 success, 7월 백필 success, last_collected_at 갱신 확인.
- 후속(74629f9) **스케줄 시간대 버그 수정**: config/app.php timezone이 'UTC' 하드코딩이라 운영 .env의 `APP_TIMEZONE=Asia/Seoul`이 무시됨 → 06:00 스케줄이 실제로는 15:00 KST에 실행되고 있었음. env 연동으로 수정해 정기 수집(06:00)·익명화(03:00)·휴지통 퍼지(03:30/03:35)가 KST 기준으로 실행. 전환일(07-31) 몫은 3채널 수동 수집으로 보전(07-30: 네이버 ₩4,576·메타 ₩38,484·구글 ₩6,761).

## 31. 2026-07-31 '계약 현황' → '매출 현황' 개칭 + 수금 진행률 기간 필터 왜곡 수정

- **메뉴 개칭(프론트 f9f3f07·22c0a2f / 백 6be4112)**: 표의 행이 월별 매출 인식 회차이고 화면 목적이 매출 기록·집계라 '계약 현황' → **'매출 현황'** 으로 변경(사이드바·제목·탭·휴지통 백링크). 날짜 라벨 **'계약일' → '매출일'** 통일(표·다이얼로그·CSV·정렬·이력 필드). 계약 개념은 프로젝트(계약 그룹)가 담당.
- **수금 진행률 수정(백 c5e6196 / 프론트 c76a698)**: 기간 필터로 회차가 잘리면 화면 행만 합산해 3회차 완납 프로젝트가 '30% · 1회'로 표기되던 버그(세계로치과 '디지털사이니지' 사례). 서버가 `group.paidAmount/paidCount`(취소 제외, **그룹 전체 회차**)를 계산해 내려주고 화면은 그대로 표기 — index는 withSum/withCount 프리로드, 단건 응답은 toFrontend 집계. 검증: 7월 필터에서 로컬 재현(3회 분납) 100%·3회, 운영 세계로치과 100%·3회 확인.
- 후속(a97e566) **미도래 회차 제외**: 미래 일정(2028-04까지)을 '완료'로 일괄 등록한 프로젝트가 98%·22회로 표기(동래센트럴 '스마트LED배너') → 수금 정의를 **완료 상태 + 매출일이 오늘까지 도래한 회차**로 한정. 수정 후 50%·3회(₩2,200,000)로 정상. 데이터 정리: 해당 프로젝트 미래 회차 11건(2026-08~2027-06)을 '진행 중'으로 일괄 전환(이력 기록) — 미래 월 집계에 확정 매출로 잡히던 문제 해소, 전체 DB 기준 미래 '완료' 회차 0건 확인.
- 후속(fcb83e0) **수정 트리거 이동**: 금액 클릭 편집 → 액션 열 연필 버튼(휴지통 옆). 행 전체(매출일·금액·비용·상태·프로젝트)를 수정하는 팝업이라 트리거 위치 명확화. 미입력 배지는 CTA 유지, 팝업 제목 '매출 수정'.
- 후속(801894e): **예상 매출 집계 기준 확장** — '진행 중'만 → **'완료+진행 중'** 합계(취소만 제외). 카드 배지·하단 안내문 동일 갱신. 순매출 카드(완료 기준)는 변경 없음.

## 32. 2026-07-31 청구서·지출결의서 체크 — 미수금·발생 기준 지출 (A안, #39 완료 / 백 fc39aec / 프론트 f29ccad)

- **설계(A안)**: 체크 = 서류·행정 사실, 상태 = 입금 사실 — 역할 분리로 상태와 중복 없음. `contracts.invoiced`(청구서 작성·전달)·`expense_requested`(지출결의서 작성·전달, 지급 확정 취급) boolean 2개.
- **지출 발생 기준**: 지출 인정 = 완료 회차 또는 결의서 전달된 회차(취소 제외, `scopeExpenseRecognized`) — "지출은 나갔는데 입금 전에 달이 넘어가는" 회차의 비용이 그 달 순매출에서 차감됨. 적용 계층: 매출 현황 요약, 추이 margin(currentProfit), 월 스냅숏 total_cost, 보고서 cost/netRevenue, 채널 기여도 net.
- **미수금**: 청구서 전달 + 상태 '진행 중' 회차 = 청구 후 입금 대기 → 요약 5카드에 미수금(₩합계·n건, 경고색) 추가, 해당 행 청구서 칩 경고색.
- **UI**: 표에 '청구/결의' 열 — 토글 칩(클릭 전환, 결의서 칩은 비용 입력 회차만), 수정 팝업 체크 2개, CSV 열 2개 추가, 변경은 '청구서'/'지출결의서' 이력 기록. 과거 월 회차의 결의 체크 변경 시 해당 월 스냅숏 자동 재계산.
- 검증(로컬): 회차를 진행 중+청구+결의로 전환 → 순매출 ₩34,385,924→₩27,385,924(매출 −700만, 결의 지출 100만 유지), 미수금 ₩7,000,000·1건, 칩 색상(경고/초록) 확인. 칩 클릭 토글 실시간 재계산(결의 해제 시 지출 −100만). 서버 currentProfit 동일 값·원복 확인. 프로덕션 MySQL 마이그레이션(300ms DONE)·스모크 200.

## 33. 2026-08-02 정기/비정기 매출 구분 — 정기 관리 탭 + 월액 변경 이력 (백 a5200a1 / 프론트 fae8984)

- **입력**: 매출 직접 입력에 매출 유형(비정기/정기) 토글. 정기 = 서비스명·기간(시작월~종료월)·월 매출금액·월 비용(선택)·매출일(1~28일) 입력 → 서버가 매월 회차 자동 생성(최대 60개월), 개월 수·기간 합계 미리보기.
- **스키마**: `contract_groups`에 recurring·recurring_start_month/end_month·monthly_amount·billing_day·recurring_status 추가, `recurring_rate_logs`(이전→새 금액, 적용 시작월, 메모, actor, 일시) 신설. 회차는 기존 contracts 그대로 → 순매출·수금 진행률·스냅숏 집계 자동 반영.
- **월액 변경**: PATCH `/recurring-plans/{group}/rate` — 적용 시작월 이후 회차만 일괄 변경, 이전 회차 보존, 불변 이력 기록, 과거 월 스냅숏 재계산. 그룹 총액(표시용)도 회차 합으로 갱신.
- **정기 관리 탭**(`/revenue/recurring`, RevenueTabs 2번째): 플랜 목록(기간·매출일·현재 월액·'변경 n회' 칩·수금 n/N회·상태) + 요약 카드(진행 중 건수·월 정기 매출 합). 행 클릭 → 월액 변경 다이얼로그 + 이력 전체(변경자·일시·메모) 표시. 종료월 경과 시 표시 상태 '종료'.
- **연계**: 매출 현황 표 정기 그룹에 보라 '정기' 칩, 수정 팝업에 정기 회차 안내 배너("이 회차만 수정 — 일괄 변경은 정기 관리에서").
- 검증(로컬 UI): 정기테스트업체 2026-10~2027-10 월 110만 → 13회차 생성, 월액 220만(2027-01부터) 변경 → 총액 ₩25,300,000(110만×3+220만×10), 이력 2건(최초 등록·변경, 김민준·일시·메모) 확인 후 테스트 데이터 정리.

## 34. 2026-08-02 매출 Top 거래처 위젯 이동 — 대시보드 → 추이·기여도 분석 (프론트 e284022)

- **이동**: 통합 대시보드의 '매출 Top 거래처' 카드를 매출 관리 › 추이·기여도 분석으로 이동(채널 기여도 아래 전체 폭). "어느 채널이 벌었나"(채널 기여도)와 "어느 거래처가 벌었나"(Top 거래처)를 같은 분석 화면에 배치.
- **필터 연동**: 페이지의 기간 필터(from/to 월)·스코프 탭(전체/신규 유입/기존)을 그대로 적용 — 하드코딩 연도(2026) 제거, 기본 올해 누적. 라벨에 기간·스코프 병기.
- 컴포넌트: components/dashboard/top-clients → components/revenue/top-clients. 대시보드는 목표·KPI·매출 추이 → 광고 성과 흐름으로 단순화.

## 33. 2026-08-02 정기 매출 + Top 거래처 위젯 이동 (#34 완료 / 백 a5200a1·75b3867 / 프론트 fae8984·e284022)

- **정기 매출**: contract_groups 확장(recurring·기간·월액·매출일·상태) + recurring_rate_logs(불변 이력). 매출 직접 입력에 비정기/정기 토글 — 정기는 기간(시작월~종료월, 최대 60개월)·월액·매출일(1~28일)로 **매월 회차 자동 생성**(개월 수·기간 합계 미리보기).
- **정기 관리 탭**(/revenue/recurring): 플랜 목록(기간·현재 월액·수금 진행 n/m회·상태), 행 클릭 → **월액 변경**(적용 시작월 이후 회차만 일괄 변경, 이전 회차 금액 보존, 변경자·일시·메모 불변 이력) + 과거 월 스냅숏 재계산. 종료월 경과 시 표시 상태 '종료'(해지와 구분).
- 매출 현황 표: 정기 그룹 보라색 '정기' 칩, 수정 팝업에 정기 회차 안내 배너.
- **검증 수정(75b3867)**: 정기·분납 생성 회차가 미래 월까지 '완료'로 만들어지던 것을 **미도래 회차 = '진행 중'** 으로 교정(§31 미도래 규칙 정합 — 스마트LED배너 재발 방지).
- **Top 거래처 위젯**: 대시보드 → 추이·기여도 분석으로 이동(채널 기여도 아래 전체 폭), 기간 필터·스코프 탭 연동, 하드코딩 연도 제거.
- 검증: 정기 등록(6~10월·월 50만) → 회차 5건(도래 2건 완료·미도래 3건 진행 중), 월액 변경(9월부터 60만) → 9·10월만 변경·이력 2건·총액 재계산(₩2,700,000), 정기 관리 목록 수금 2/5회 ₩1,000,000 표시, 프로덕션 MySQL 마이그레이션(584ms DONE)·스모크 200/401. 테스트 데이터 원복.

## 34. 2026-08-03 업무별 담당자(프로젝트 담당자) + 주 담당자 변경 관리자 전용 (백 e61c6d3 / 프론트 308b21a)

- **프로젝트(업무) 담당자**: 같은 고객이라도 업무별 담당자가 다른 현실 반영 — `contract_groups.owner_id`(선택). 표시·권한은 **프로젝트 담당자 → 고객(주) 담당자** 폴백(매출 현황 담당자 열·정기 관리·계약 삭제 권한). 매출 직접 입력·수정 팝업의 프로젝트 섹션에 담당자 셀렉트(비우면 고객 담당자), 변경 시 최근 회차에 '프로젝트 담당자' 불변 이력.
- **주 담당자 변경 = 관리자 전용**: 유입 고객 상세 담당자 셀렉트·changeOwner API(403 가드), 기존 고객 수정 팝업 주 담당자 셀렉트(비관리자 비활성 + 안내). 이력 — 유입은 기존 ChangeLog('담당자'), 기존 고객은 메모 불변 주석(`[담당자 변경 날짜] A → B (변경: 관리자)`, 병합 기록과 동일 관례).
- 검증: 프로젝트 담당자 지정 생성(고객=관리자·프로젝트=이수진) → 변경(→박태호, 이력 기록) → 표 담당자 열 폴백 표시 확인, 기존 고객 주 담당자 변경(관리자 OK+메모 이력 / staff 403), 유입 changeOwner staff 403. 프로덕션 MySQL 마이그레이션(231ms DONE). 테스트 데이터 원복.

## 35. 2026-08-03 ROAS 표기 % 통일 (프론트 9ee6167)

- 전 화면 ROAS를 배수(`5.53배`) 대신 **%(`553%`)** 로 표기 — `fmtRoas` 공용 포매터(값은 비율 유지, 표기만 ×100 정수). 적용: 대시보드 KPI·ROAS 추이(호버 포함)·채널 비교/채널·캠페인 상세·추이 분석 툴팁·보고서 뷰어/버전 비교.

## 36. 2026-08-03 신규 유입 → 기존 고객 전환 (백 eea58eb / 프론트 8120896)

- **기준**: 광고 유입으로 시작된 **첫 거래(완료 계약)가 있는 고객**은 기존 고객으로 전환 가능 — 전환 후의 새 거래는 기존 매출(ROAS 미포함), 과거 계약·지표·보고서는 **불변**(유형이 계약에 저장되어 조회 시점 무관 동일 결과).
- **전환**: 고객 상세 '기존 고객으로 전환' 카드(계약 단계 노출, 담당자·관리자, 확인 다이얼로그) → 기존 고객 자동 생성(병원명→업체명, 담당자명→고객명, 연락처·담당자 승계, merged_from 링크·메모 전환 주석) + 유입 이력에 '기존 전환' 불변 기록. 전환 후 상세에 배지 + 기존 고객 상세 상호 링크. 중복 전환 422.
- **입력 가드**: 매출 직접 입력에서 전환된 고객을 신규 유입으로 선택하면 **기존 탭 자동 전환 + 해당 기존 고객 프리필** + 안내 토스트 — 재거래가 신규 유입으로 잘못 입력되는 것 방지.
- 검증: 전환(기존 고객 생성·이력·상호 링크), 중복 전환 422, 입력 가드(자동 탭 전환·프리필·토스트) 확인. 테스트 전환 원복. 프로덕션 스모크 200/401.
- 핫픽스(e304d7a): 전환 다이얼로그 상태 훅이 로딩 조기 반환 뒤에 선언돼 상세 페이지가 client-side exception으로 크래시 → 상태 선언부로 이동.
- **개정(08-03, 백 a472f21 / 프론트 4fa1dc5) — 기준을 '계약 성립 즉시'로 + 프로세스 직결**:
  - 자동 전환: 신규 유입 매출 등록 시 서버가 자동 전환(LeadConversion, 멱등). 동일 업체명 기존 고객이 있으면 신규 생성 대신 연결(중복 방지). 첫 프로젝트 잔여 회차는 계속 신규 유입 귀속 — ROAS·신규/기존 분해 불변.
  - 입력 선택지: 전환된 고객을 신규 유입 탭에서 선택하면 오버레이로 "첫 프로젝트 회차 추가(신규 유입 귀속) / 새 거래(기존 프리필)" 선택.
  - 계약→매출 직결: 단계 '계약' 처리 토스트에 '매출 바로 입력' 액션 + `/revenue?input=<유입고객ID>` 딥링크로 입력 팝업 자동 오픈·프리필. 고객 상세 계약 카드도 동일 링크.
  - **과거분 일괄 전환**: 계약 기록(취소 제외) 보유 유입 고객 33명 전환 완료(로컬·운영 동일) — 그중 동일명 연결 2건(해운대호한의원·세계로치과병원). 7월 신규 유입 매출 ₩16,870,000 전후 동일(지표 불변 검증).

## 37. 2026-08-03 혼합형 정기 계약 — 초기 1회 금액 + 월 정기 (백 5e7789b / 프론트 5076509)

- **모델**: "초기 금액(기계·설치 등 1회) + 월 정기 서비스"가 한 계약인 경우(스마트LED배너형) 지원 — 정기 입력에 **초기 금액·초기 비용(선택)** 추가. 초기 회차는 시작월 1건, 월 회차는 **익월부터** 생성. 계약금액(총액) = 초기 + 월액×월 회차 수 자동 계산.
- **월액 변경 보호**: 정기 관리에서 월액을 바꿔도 초기 회차(시작월)는 변경 대상에서 제외. 다이얼로그에 초기 금액 병기·보호 안내, 매출일 미지정 플랜은 '말일' 표기.
- **스마트LED배너 마이그레이션(운영)**: 분납형으로 통짜 입력돼 있던 프로젝트를 혼합형 정기로 전환 — recurring 메타(2026-07~2028-04, 월 ₩110,000, 초기 ₩2,200,000) + 최초 월액 이력. 회차 데이터는 불변(수금 ₩2,200,000 · 3회 유지), 이제 정기 관리 탭에서 월액 변경·이력 관리 가능.
- 검증(로컬): 혼합 등록(초기 220만+월 11만, 7~12월) → 회차 6건(7월 초기 완료, 8~12월 11만 진행 중)·총액 ₩2,750,000, 시작월 포함 월액 변경(15만) 시 초기 회차 보호·총액 ₩2,950,000 재계산. 테스트 원복. 프로덕션 마이그레이션(286ms DONE)·스모크 200.

## 38. 2026-08-03 담당자 선택지 실계정 단일 소스 통일 (백 f0ac899 / 프론트 43cff48)

- **증상**: 기존 고객 수정 팝업의 주 담당자 등 일부 셀렉트에 갱신된 담당자명이 안 뜸.
- **원인 2가지**: ① 담당자 선택지가 실계정(useOwners → GET /owners)과 하드코딩 목록(OWNERS: 김민준·이수진·박태호) 두 소스로 갈라져 있었음 ② /owners가 팀장·담당자 역할만 반환해 관리자·대표 계정 누락.
- **수정**: 하드코딩 사용처 5곳(기존 고객 필터·주 담당자 셀렉트, 매출 입력·수정 팝업 프로젝트 담당자, 수기 입력 기본값)을 useOwners로 통일하고 useOwners의 하드코딩 폴백 제거 — **설정 › 사용자 권한에서 계정을 추가·변경하면 전 화면 드롭다운에 자동 반영**. /owners는 활성 계정 전체(관리자·대표 포함)로 확장. 셀렉트는 현재값 union으로 목록 밖 과거 담당자도 표시 유지.
- 검증: /owners 5계정(관리자·김민준·박태호·이수진·정하늘) 반환, 기존 고객 담당자 필터에 전원 표시 확인.

## 39. 2026-08-03 유입 코호트 전환율 위젯 (백 a6b1245 / 프론트 272c81b)

- **정의**: 3개월 이동 코호트(유입월 창) × 리드별 90일 관찰 창 — "최근 3개월 유입 고객 중 유입일로부터 90일 안에 계약(취소 제외)한 비율". 창 설정 근거는 리드타임 실측(전환 32명: 중앙값 25일, 1개월 내 56% · 3개월 내 88% · 4개월 +3%p뿐 / 월 유입 5~10건이라 월 코호트는 노이즈 과다 → 3개월 묶음 N≈20).
- **표시**: 추이·기여도 분석 하단에 최근 12개 이동 코호트 막대 — 유입·계약 건수, 전환율%. 관찰 90일이 안 끝난 코호트는 **'잠정' 배지 + 연한 막대**(수치가 더 오를 수 있음 안내).
- 기존 퍼널(기간 스냅샷)의 대체가 아니라 병행 — 퍼널=현재 파이프라인 상태, 코호트=유입 품질 평가. 기간 필터와 무관하게 항상 최근 12개.
- 검증: 실데이터 12개 코호트 렌더(확정 11%→38% 상승 추세, 최근 4개 잠정), 프로덕션 스모크 200.

## 40. 2026-08-04 세부 유입경로 구조화 (백 03443eb / 프론트 c7e62d9)

- **문제**: CSV 유입경로(블로그·검색·홈페이지·메타·BDEX·이벤트·소개…)가 임포트 시 5개 대분류로만 저장되고 원본 표기는 memo 텍스트로만 남아 필터·집계 불가. 결과적으로 온라인 유입 79건이 '온라인 비광고' 한 덩어리가 되고, 광고 퍼널은 '온라인 광고'(메타 1건)만 대상이라 사실상 비어 보였음.
- **수정**: `customers.source_detail`(nullable·인덱스) + `KoLabels::SOURCE_DETAILS`(블로그·검색·홈페이지·메타·BDEX·이벤트·소개·기타) 신설. **소개는 세부(직원/업체/클라이언트/업체명)를 합쳐 '소개' 하나로** (사용자 확정). 임포터가 세부 분류를 저장하고, 목록 필터·수기 입력·편집(변경 이력 포함)에서 사용.
- **백필**: `customers:backfill-source-detail` — memo의 `유입(원본): X` 표기로 기존 데이터 복구. 운영 118건 백필(홈페이지 29·검색 28·소개 27·블로그 22·이벤트 6·BDEX 4·메타 1·기타 1), 원본 표기 없는 7건은 건너뜀.
- **화면**: 유입 고객 목록의 유입 경로 셀에 세부 경로 배지 + '세부 경로' 필터(URL 파라미터 연동), 상세 기본 정보 배지, 편집 팝업·수기 입력에 세부 경로 셀렉트.
- 검증: 필터 API 건수 일치(블로그 22·검색 28·홈페이지 29·소개 27·BDEX 4·이벤트 6·메타 1), 프로덕션 마이그레이션(505ms)·백필·스모크 200.

## 41. 2026-08-04 문의 서비스 9개 통합 카테고리 (백 42270f2 / 프론트 6016a07)

- **문제**: CSV의 문의 서비스(2025 '문의서비스' / 2026 '문의 내용')가 DB에 저장되지 않고 memo 텍스트로만 남아 "어떤 서비스로 문의가 들어오는가"를 집계할 수 없었음. 두 해 표기가 다르고("정기마케팅/온라인마케팅(건)" vs "마케팅", "웹사이트" vs "홈페이지") 복수 선택("마케팅, 홈페이지")이 흔한 것이 통합의 걸림돌.
- **카테고리(9종 확정)**: 마케팅 · 홈페이지 · CI·사인물 · LED·DID · 영상 · 디자인 · 인쇄물 · 컨설팅·의료심의 · 기타.
- **구현**: `customers.services`(JSON 배열 — 복수 선택 지원) + `KoLabels::SERVICE_CATEGORIES` + `ServiceClassifier`(표기 차이·복수 파싱 단일 소스, 임포터·백필 공용). 목록 필터는 `whereJsonContains`, 수기 입력·편집은 토글 칩(복수), 변경 시 '문의 서비스' 이력 기록.
- **백필**: `customers:backfill-services` — memo의 `서비스:`(2025)/`문의:`(2026) 표기 파싱. 운영 111건 백필 — 마케팅 44 · CI·사인물 29 · 홈페이지 21 · 영상 8 · LED·DID 8 · 디자인 7 · 인쇄물 6 · 컨설팅·의료심의 4 · 기타 3, 복수 선택 16건. 원본 표기 없는 14건은 건너뜀.
- **화면**: 유입 고객 목록 병원명 아래 서비스 배지 + '문의 서비스' 필터, 상세 기본 정보 배지, 편집·수기 입력 토글 칩.
- 검증: 분류 합계가 CSV 원본과 일치(마케팅 44 = 2026년 26 + 2025년 18), 필터 API 44건, 화면 배지·필터 동작. 프로덕션 마이그레이션(61ms)·백필·스모크 200.

## 42. 2026-08-04 Marketing ROAS 전환 (#44 완료 / 백 6893949 / 프론트 ec8fe30)

- **정의 확정**: `Marketing ROAS = 신규 유입 매출 ÷ 온라인 총 광고비(네이버+메타+구글)`. 전사 ROAS는 이미 이 계산이었고 이름만 'ROAS'라 채널 귀속 기반으로 오해되던 것을 바로잡음 — **계산 로직 무변경이라 과거 수치도 그대로**. 대시보드 KPI·ROAS 추이·추이 분석·보고서·버전 비교 라벨 통일 + 정의 병기.
- **채널별 ROAS 비활성**: 확정 귀속이 125명 중 1명(0.8%)이라 채널 단위 ROAS는 사실상 0으로만 표시돼 오해를 유발. `ADS_CHANNEL_ROAS_ENABLED`(기본 false) 플래그로 끄고 서버가 null 반환 → 채널·캠페인 상세에서 '—'(측정 불가), 합계도 함께 비활성. 계산식·코드는 보존해 귀속 개선 시 플래그만 켜면 복구.
- **3개월 이동 병기**: 리드타임 중앙값 25일이라 '7월 광고비 vs 7월 계약'은 서로 다른 코호트 — 예산 증감 직후 착시 발생. `roas3m`(직전 3개월 매출합÷광고비합)을 추이 분석 툴팁에 병기. 실측 예: 6월 월별 4,348% vs 3개월 2,747%, 7월 3,860% vs 3,055%.
- 검증: API 3개월 값 산출 확인, 화면 라벨·툴팁 병기·채널 '—' 표기 확인. 프로덕션 스모크 200.

## 43. 2026-08-04 온라인 유입 퍼널 — 대상 확장 + 세부 경로별 전환 (백 4cb4a03 / 프론트 0de544d)

- **문제**: 퍼널 대상이 `source_category='online_ad'`뿐이라 실데이터에서 메타 1건만 잡혀 사실상 비어 있었음. 우리 온라인 유입의 실질은 블로그·검색·홈페이지(비광고).
- **수정**: 대상을 **온라인 유입 전체(online_ad + online_organic)** 로 확장 → 80명이 퍼널에 들어옴. 메뉴·제목을 '광고 퍼널' → **'온라인 유입 퍼널'** 로 개칭. 광고 클릭은 광고 채널만의 지표이므로 '참고'임을 안내문에 명시(비광고와 분모 상이).
- **세부 경로별 계약 전환 카드**: 어느 온라인 경로가 실제 계약까지 이어지는지 막대로 표시. API에 `byDetail`(유입·계약·전환율) 추가, `source_detail` 파라미터로 경로별 퍼널 조회도 지원.
- 실측(전체 기간): 온라인 문의 80 → 상담 80 → 견적/제안 44(55%) → 계약 19(43.2%). 경로별 — 홈페이지 29건 중 7건(24.1%) · 검색 28건 중 7건(25%) · 블로그 22건 중 5건(22.7%) · 메타 1건 중 0건.
- 검증: API·화면 수치 일치, 프로덕션 스모크 200.

## 44. 2026-08-04 메뉴 재편 — 역할 기준 6그룹 (프론트 6523874)

- **배경**: 매출 현황 세분화(프로젝트·분납·정기·청구/결의)로 초기 기획의 "입력처 기준" 메뉴가 실사용과 어긋남 — 탭·사이드바 불일치(정기 관리 사이드바 누락), 고객이 두 메뉴에 분산, 분석이 3개 대메뉴에 산재, 액션(수기 입력·업로드)이 메뉴화.
- **새 구조**: `대시보드(단일) / 고객 관리(유입·기존·미입력/지연) / 매출 관리(현황·정기·목표) / 성과 분석(퍼널·추이·기여도·채널별·캠페인별) / 보고서 / 설정`. **URL 경로는 전부 유지** — 북마크·딥링크·문서 안 깨짐.
- **불일치 구조적 차단**: `group-tabs.tsx`가 단일 소스 — 사이드바 NAV가 탭 정의(CUSTOMER/REVENUE/ANALYTICS_TABS)를 그대로 소비. 구 RevenueTabs·DashboardTabs 삭제.
- **이동·흡수**: 기존 고객 목록 → 고객 관리(자동 전환 후 "고객은 한 곳" 원칙, 상세 직행 링크 4곳은 무영향 확인). 채널별·캠페인별 → 성과 분석(채널 ROAS 비활성 후 실질이 광고 운영 지표). 수기 입력·파일 업로드 → 유입 고객 목록 버튼, 휴지통 2개 → 각 목록 우상단 아이콘, 온보딩 → 헤더 도움말 아이콘. 설정의 목 화면(개인정보·시스템)에 '준비 중' 배지.
- **백업**: 재편 직전 상태를 양쪽 리포 `backup/pre-menu-restructure-20260804` 브랜치로 보존 (프론트 0de544d / 백엔드 4cb4a03).
- 검증: 사이드바 6그룹, 그룹별 탭·활성 표시·브레드크럼, 버튼 흡수, 배지, 온보딩 메뉴 제거·헤더 아이콘 — 브라우저 확인. 프로덕션 5개 경로 스모크 200.

## 45. 2026-08-04 알림 실이벤트 전환 (프론트 0dbd3b3)

- **문제**: 종 아이콘 배지·알림 센터가 전부 클라이언트 목 데이터 — 실제 상태와 무관한 가짜 알림(토큰 만료·목표 미달·소급 반영 등)이 실데이터 운영 화면에 노출.
- **전환**: 서버 실데이터 2종만 표시 — ① **수집 실패/지연**(GET /collection/status, 상단 신뢰도 바와 동일 소스) ② **입력 지연**(GET /overdue, 유입 후 24시간 초과 미입력 건수). `src/lib/alerts.ts` 단일 소스.
- **성격**: 이벤트 피드가 아니라 **"현재 조치가 필요한 상태" 알림** — 해결되면 자동으로 사라지므로 읽음 상태·보관 없음('모두 읽음' 제거). 백엔드 이벤트 저장이 생기면 목표 미달 등 나머지 카테고리 복원 검토.
- **화면**: 헤더 배지 = 조치 필요 건수(60초 폴링), 알림 센터 항목 클릭 시 조치 화면(광고 채널 연동/미입력 현황) 이동, 0건이면 "조치가 필요한 알림이 없습니다". mock-settings의 목 알림 블록 제거.
- 검증(로컬): 수집 실패 1·지연 2 조건에서 배지 3 = 항목 3 일치, 미입력 24h 초과 0건이라 입력 지연 알림 미표시(조건부 정확). 프로덕션 스모크 200.

## 46. 2026-08-05 광고 채널 연동 실데이터 전환 + 외부 매출 연동 보류 (백 9346964 / 프론트 07fd7d6)

- **문제**: 설정 › 광고 채널 연동이 초기 시안(M6) 목 데이터 그대로 — 연동 카드가 하드코딩 상태(실패·토큰 만료)를 표시하고 '재연결' 버튼은 화면 배열만 바꾸는 가짜. 알림이 조치 화면으로 보내는 페이지가 목이었음. 실패 이력도 가짜 3건 고정.
- **연동 카드**: 실 수집 상태(`/collection/status` — 신뢰도 바·알림과 동일 소스)로 전환. 채널별 수집 완료/지연/실패 + 마지막 수집 시각 + 토큰 만료 경고. 버튼은 **실제 재수집 API**(하루 3회 제한 서버 관리, 남은 횟수 표시, 소진 시 비활성).
- **수집 이력**: 백엔드 `GET /collection/runs` 신설 — collection_runs 실데이터(최근 90일·최신순·정기/수동 구분·오류 메시지). 운영 93건.
- **수기 입력 설명 갱신**: "API 미연동 채널" → "3채널 자동 수집 중 — 과거 월 보정·누락 보완용".
- **외부 매출 연동 보류**(사용자 결정, 투두 #45): 가짜 저장 폼 제거 → '준비 중' 안내로 비활성. 재개 시 파일 업로드(A안) → 서비스 계정 시트 연동(B안) 순, 보안 검토 내용은 투두에 기록.
- 검증: 로컬(실패 1·지연 2 상태 그대로 표시, 이력 11건), 운영(3채널 수집 완료·이력 93건) 확인, 스모크 200.

## 47. 2026-08-05 사용자 권한 페이지 A안 — 목 매트릭스 제거 (#46 완료 / 프론트 217d42a)

- **문제**: 하단 권한 매트릭스가 목 사용자 5명(실계정 무관)의 기능 토글 — 서버 미반영·새로고침 소실, 변경 이력도 가짜. 백엔드에 기능 플래그 권한 시스템 자체가 없어 "존재하지 않는 시스템의 UI"였음.
- **수정(A안)**: 매트릭스·가짜 이력·mock-settings F2 블록 제거 → **'역할별 권한 안내' 카드**로 대체. 서버가 실제 검증(403)하는 규칙 6종을 그대로 문서화: 계정 등록·비번 초기화·주 담당자 변경 = 관리자 / 고객·계약 삭제·수동 전환 = 담당자 또는 관리자 / 입력·수정·목표·보고서 = 전 구성원(이력 기록).
- **유지**: 계정 관리 섹션(실 API — 목록·등록·비밀번호 초기화, 마지막 로그인·비밀번호 상태 표시).
- 세분 권한 실구현(B안)은 조직 규모상 보류 — 필요 시 백엔드 구현과 함께 복원.
- 검증: 실계정 5명 목록·규칙 6행 렌더, 목 이름 잔재 없음, 프로덕션 스모크 200. 이로써 목 화면 잔여는 개인정보 정책·시스템 설정('준비 중' 배지)뿐.

## 48. 2026-08-05 코호트 위젯 세로 막대 전환 (#43 완료 / 프론트 774fd58)

- **문제**(사용자 피드백): 가로 막대 12행은 시간 순서가 위→아래로 읽혀 추세가 안 보이고, 막대 길이가 최대값 대비 상대 길이라 절대 % 감이 안 잡힘.
- **수정(A안)**: 시간축(왼→오) 세로 막대 차트 — 추세가 막대 높이의 우상향/하향으로 즉시 읽힘. **% 라벨 상시 표기**(길이 해석 불필요), x축에 코호트 종료월(~26.04)과 유입 수(n=21) 병기. 잠정 코호트는 연한 막대+주황 라벨(범례 포함). 호버 시 하단에 "26.02~26.04 유입 · 유입 21건 중 90일 내 계약 8건 = 38%" 형식 상세. 순매출 추이·ROAS 차트와 동일 문법.
- 검증: 12개 막대·%·n 라벨 렌더, 호버 상세 동작, 프로덕션 스모크 200.

## 49. 2026-08-05 대시보드 채널 비교·비중 3개월 공통 기간 (백 1155552 / 프론트 adb0816·이후 라벨 픽스)

- **채널별 성과 비교**: 당월 지표 → **선택 월 포함 최근 3개월 합산** (사용자 요청, 기간은 3개월 추천 채택 — 코호트·Marketing ROAS 3개월 이동과 동일 문법, 월 단위 출렁임 완화). 부제에 '최근 3개월 합산 · 6~8월' 기간 병기.
- **지표 탭 교체**: 비활성 ROAS 탭('—'만 표시되던 죽은 탭) → **전환수** (채널 API가 보고한 전환 이벤트 합). 채널마다 전환 정의가 달라(구글 다수·메타 0) '문의수' 대신 '전환수'로 표기하고 툴팁으로 "절대 비교보다 추이 참고용" 안내.
- **채널 비중 변화**: 6개월 → 비교와 **동일한 3개월 창**의 월별 광고비 비율 (부제에 기간 병기). 서버가 channelComparePeriod('6~8월') 라벨 제공 — 두 위젯이 항상 같은 기간을 보장.
- 검증: 로컬·운영(6~8월 — 구글 광고비 55만·전환 573, 메타 165만·0, 네이버 162만·2, 비중 3개월) 확인, 스모크 200.

## 50. 2026-08-05 채널별 성과 비교 — 월별 그룹 막대 (백 8a1ef5f / 프론트 b9a7e98)

- **§49의 3개월 합산을 월별 분해로 개정**(사용자 의도 확인): 질문이 "어느 채널이 큰가"(→ 비중 변화가 답)가 아니라 **"어느 채널이 변하고 있나"**. x축=월(선택 월 포함 3개월), 월마다 채널 3색 막대 — 채널 비중 변화와 창·축·색이 동일해 두 위젯이 위아래로 함께 읽힘.
- 지표 탭 광고비/CTR/CPC. **전환수 탭 제거** — 채널마다 전환 정의가 달라(구글 전환 이벤트 573 vs CRM 문의 125) 비교 부적합, 유입 성과는 온라인 유입 퍼널 소관.
- 호버 시 월별 정확값, 광고비 탭 기본 표시줄에 3개월 채널 합계.
- 검증: 로컬(6~8월 축·탭 3개·호버 "7월 네이버 ₩437,080…"), 프로덕션 스모크 200.

## 51. 2026-08-06 영업 파이프라인 개편 + 90일 무활동 자동 보류 (백 3db7498 / 프론트 5c11169)

- **파이프라인 카드 개편**(사용자 요청): 대표 숫자 = **해당 월 신규 문의**('N월 기준' 명시), 진행 중(상담·견적/제안)은 아래 보조 표기('전체 기간 · 보류 제외' 명시) + 보류 건수 병기.
- **자동 보류**: `customers.held_at` + `customers:auto-hold`(매일 06:10) — **유입 후 90일 경과 && 최근 90일 내 단계 변경 이력 없음** → 자동 보류(변경 이력 "자동 보류 — 90일 무활동"). 계약 도달 건 제외. **90일 = 코호트의 리드별 관찰 창과 동일 기준**(달력 3개월 아님 — 유입 시점 무관 동일 대우, 사용자 확정). 상태명은 '취소'가 아닌 '보류'(자동화는 사실만 기록).
- **재개**: 단계 변경 시 서버가 자동 재개 + 상세의 보류 배지에서 원클릭 수동 재개(모두 이력 기록).
- **화면**: 유입 목록 기본 = 진행 중만, '보류 보기' 토글로 보류 조회. 대시보드 퍼널 집계는 보류 제외.
- **소급 일괄 보류(운영)**: 80건 처리(안전장치로 최근 단계 변경 보유 건 보호) → 파이프라인 93 → **13건**(상담 2·견적/제안 9·유입 2), 계약 도달 33건은 완결로 별도. 코호트·퍼널 등 과거 지표는 계약 이력 기준이라 불변.
- 검증(로컬): 자동 보류 79건·활성 43, held 필터, 수동 재개(이력 '재개 — 수동')·원복. 프로덕션 마이그레이션(537ms)·일괄 보류·스모크 200.

## 52. 2026-08-06 대시보드/분석 역할 분리 마무리 (백 42231ae / 프론트 a0d0984)

- **역할 선언 완성**: 대시보드 = "지금 상태 + 오늘 챙길 것", 성과 분석 = "기간을 바꿔가며 해석". 메뉴 재편(§44)에서 시작한 분리의 잔여분.
- **조치 필요 카드 3장 신설** (KPI 행 아래): ① 미수금(청구 후 입금 대기 — 건수·금액, 운영 1건 ₩330,000) ② 유입 미입력 24시간 초과 ③ 광고 수집 이슈(지연·실패 채널 수). 각 카드 클릭 시 조치 화면(매출 현황/미입력 현황/채널 연동) 이동. **0건이어도 초록 '없음'으로 표시** — 카드가 사라지면 확인 여부를 알 수 없으므로.
- **Marketing ROAS 추이 차트 제거** → 성과 분석 › 추이·기여도로 위임(그쪽 이중축 차트가 동일 정보 + 3개월 이동 병기까지 제공). 중복 위젯 4종 중 마지막 정리 — 목표/달성률(목표 진행 카드는 '이번 달 상태'라 잔류), 매출 추이(페이스 확인용 1개 잔류), 채널(3개월 창으로 위젯 자체를 재정의), ROAS 추이(제거).
- 채널 비교·비중을 한 행 2열로 재배치, 광고 성과 안내문을 성과 분석 메뉴 기준으로 갱신. roas-trend.tsx 삭제.
- 검증: 조치 카드 3장 렌더(로컬 — 수집 이슈 3건 경고·나머지 초록), ROAS 추이 차트 제거 확인, 운영 미수금 1건 ₩330,000 정확, 스모크 200.

## 53. 2026-08-13 메타 장애 시절 하드코딩 전수 제거 (#47 완료 / 프론트 6c54621)

- **배경**: 사용자 발견("네이버는 API 자동수집" 낡은 문구)을 계기로 전수 점검 — 네이버 단독 수집·메타 장애 시뮬레이션 시절의 잔재를 일괄 정리.
- **치명 버그 수정**: `metaStatus`가 "어떤 채널이든 실패하면 failed"로 정의된 채, 소비처들이 **'메타 광고'를 하드코딩으로 제외**하고 있었음 → 8/13 구글 수집 실패(Ads API v21 종료) 때 채널별 상세가 **멀쩡한 메타를 합계에서 제외**하는 오작동 실증. 채널별 상세·보고서 뷰어(신뢰도 바·채널 차트·도넛)·보고서 생성(누락 경고·제외 모드) 모두 **실패 채널 동적 판정**(`status.channels` 기준)으로 전환.
- **보고서 신뢰도 바**: "✔️ 네이버 / 메타만 상태 반영 / ✔️ 구글" 하드코딩 → 3채널 실 상태 표시, '수집 성공 N/3' 실계산. 보고서 생성의 "메타 광고 2026-07-10 데이터 누락" 가짜 고정 문구도 실패 채널 기준으로 대체.
- **설정 › 광고 채널 연동 문구 3곳**: 수기 입력·시트 업로드가 "메타·구글의 API 전환(2026-07) 이전 과거 월 보정용"임을 정확히 명시, "네이버는 API 자동수집" 표현 제거(네이버 행 스킵 사유 = API 수집분 보호).
- 정리: data-trust의 metaStatus 별칭·MetaStatus 타입 삭제(사용처 0).
- 검증: 로컬(네이버 실패 상태)에서 경고문이 실패 채널을 정확히 지목, tsc 클린, 운영 4개 화면 스모크 200.

## 54. 2026-08-13 광고 수기 입력·시트 업로드 — 소스 기준 가드 (#48 완료 / 백 88728da / 프론트 ee65b7f)

- **확인 결과(충돌 정책 실체)**: 수기 저장은 (채널·월) authoritative — 그 달 기존 행을 전부 삭제 후 1행으로 대체하는 파괴적 방식. 그런데 가드가 정확히 거꾸로였음 — **수기 입력은 isReal 채널 전면 차단**(3채널 모두 실 API인 현재 완전 무용지물), **시트 업로드는 무가드**로 API 일 단위 수집분을 삭제·덮어쓸 수 있었음.
- **재설계(소스 기준 가드)**: 채널이 아니라 데이터 소스로 판단 — 그 채널·월에 **API 일 단위 수집분(월초 1일 외 날짜 행)이 있으면** 수기/배치는 422 차단, 시트 업로드는 자동 건너뜀(skippedApi 목록 보고). 없으면(시트 월합계 구간) 어느 채널이든 보정 허용.
- **채널 제한 제거**: 네이버 포함 3채널 수기 보정 가능 — 실측상 네이버 과거 월(2025-08~2026-06)도 시트 월합계(월 2~3행)라 보정 통로가 필요했음. 구글은 전 기간 일 단위 백필이라 자연히 전 월 차단(안전).
- **화면**: 채널 셀렉트·간단 CSV에 네이버 추가, 안내문·토스트를 소스 기준 규칙으로 갱신.
- 검증: 로컬(네이버 07 차단·03 허용 후 원복), 운영(메타 2026-08 422 차단 확인 — "API 일 단위 수집분이 있어 차단", 메타·네이버 2026-03 허용 판정), 스모크 200. 연동 맵 §1의 🟡 항목 해소.

## 55. 2026-08-13 수금 월 매출 인식 + 미수금 지속 표시 (#50 완료 / 백 c0b63f7·27e135f / 프론트 a04635f)

- **선행(#52-3)**: 8개 파일에 산재하던 매출 인식(done+amount)·수금 판정 조건을 Contract 스코프 2개(revenueRecognized·paidSoFar) + revenueDateColumn()으로 단일화 — 리팩터링 전후 5개 지표 완전 일치 검증. 연동 맵 🔴3 해소.
- **수금 월 인식**: `contracts.paid_at`(수금일) 신설, 인식 월 = 수금일(revenueDateColumn 한 곳만 전환 — 선행 작업 덕에 전 집계 자동 추종). 완료 처리 시 수금일 기록(기본 오늘·팝업에서 지정), 완료 해제 시 해제, 완료 회차 수금일 단독 정정 지원(모두 이력). 과거 수금 월 변경 시 해당 월 스냅샷 재계산. **기존 완료 회차는 paid_at=contracted_at 소급 → 과거 지표 불변**(운영 384건 백필, 7월 ₩45,886,800 유지 확인).
- **수금 진행률 의미 교정**: '매출일 도래 추정' → 실제 수금(paid_at) 기준.
- **미수금 지속 표시**: 청구 후 입금 대기(invoiced+진행 중) 회차는 **기간 필터와 무관하게 목록에 항상 포함** — 지난달 청구 건이 수금될 때까지 계속 보임(예상 매출·미수금 카드에도 자동 포함).
- **화면**: 수정 팝업 완료 시 수금일 입력("이 달의 매출로 인식") + 매출 현황에서 수금 월≠매출 월이면 '수금 M-D' 병기, 규칙 안내 갱신.
- 검증(로컬 E2E): 백필 후 지표 불변 → 7월 미수금(진행 중+청구)이 8월 필터에 노출 → 완료(수금 8/13) 처리 시 8월 매출 인식·7월 불변·수금일 이력 기록. 운영 마이그레이션(506ms)·백필·지표 일치.

## 56. 2026-08-14 기존 고객 재문의 (#49 B안 / 백 ac2d64a / 프론트 c29e06b) + 구글 수집 재발 원인

- **구글 수집 재실패(08-14 06시) 원인**: 08-13 v23 수정을 `--build app`으로만 배포 → **scheduler·queue 컨테이너가 구 이미지(v21)로 잔존**. 정기 수집·큐 잡은 app이 아닌 별도 컨테이너에서 실행됨. 전체 재빌드로 해소 + 8/13 데이터 재수집(₩7,294). **배포 규칙 확정: 백엔드는 항상 전체 서비스 재빌드 + scheduler/queue CreatedAt 확인** (메모리·가이드 기록).
- **재문의 유형(B안)**: 기존 고객의 신규 업무 문의를 유입 단계 관리로 받되 유형 분리 — `inquiry_type(new|repeat)` + 기존 고객 연결. 영업 동선(챙길 문의 한 곳·단계 관리·이력·자동 보류)은 그대로, **신규 지표 3곳(신규 문의 수·코호트·온라인 유입 퍼널)에서는 제외**.
- **매출 연결**: 재문의 고객으로 신규 유입 매출 등록은 서버 422 차단, 매출 입력에서 재문의 고객을 선택하거나 딥링크로 열면 **기존 탭 + 연결 고객으로 자동 전환**.
- **화면**: 수기 입력 '신규 문의/기존 재문의' 토글(기존 고객 선택 시 병원명 자동), 목록·상세 '재문의' 배지(상세는 기존 고객으로 링크), 기존 고객 상세에 '신규 업무 문의' 섹션(단계·서비스·보류, 유입 상세로 링크).
- 검증(로컬 E2E): 등록→신규 문의 카운트 제외(0)→매출 차단 메시지(연결 고객명 포함)→기존 상세 이력 노출→원복. 운영 마이그레이션(1s)·전체 재빌드·스모크 200.

## 57. 2026-08-14 기대매출액 + 유입 상단 카드 (#53 — 주간보고 대체 1단계)

- **배경**: 주간 성과보고 슬라이드를 M-CRM 보고서로 대체하는 5단계 계획(#53~57)의 1단계. 슬라이드의 "논의 중 파이프라인(예상 매출)" 재료.
- **기대매출액**: `customers.expected_amount` — **유입관리 전용 추정치, 매출관리 화면·집계 절대 미포함**(미수금=확정 채권과 성격이 달라 이중 계상 방지). 수기 입력·상세 편집에서 입력(선택), 변경 시 '기대매출액' 이력 기록(₩ 표기).
- **C1 상단 카드 2장** (기간 필터만 반영 — 매출관리 카드와 동일 규칙): ① **신규 유입** = 재문의 제외(대시보드 신규 지표와 동일 기준) + 재문의 별도 병기 ② **기대금액** = 진행 중 파이프라인 합(재문의 포함 · 계약 도달/보류 제외) + 입력/미입력 커버리지 병기. 서버 집계(index metrics — 페이지네이션이라 클라 합산 불가).
- **매출 연동(허용 예외 한 곳)**: 계약 등록 다이얼로그에서 기대금액 있는 리드 선택/딥링크 시 "기대매출 ₩X (참고)" 배지 + 빈 첫 회차 금액 프리필 — 사람이 실제 금액으로 확정.
- **연동 맵 §9 신설**: 유입 입력 필드 추가 시 체크리스트(백 4곳·타입 3곳·화면 3곳·집계) + 기대매출액 미포함 원칙.
- 검증(로컬 E2E): 등록(1,589만)→metrics 합산→수정 이력(₩15,890,000→₩19,580,000)→보류 시 기대금액 제외·신규 수 유지→원복.
- **예정(#54~57)**: 연장률(유지율) → 건수 KPI 목표 → 클라이언트 이슈 로그 → 주간 성과보고 조립(총 예상 매출 = 확정+미수금+기대금액 합산 표시 포함).

## 58. 2026-08-14 재문의 입력 개선 — 기존 고객 검색·유입 경로 비활성화

- 재문의의 기존 고객 선택: 전체 목록 셀렉트 → **서버 자동완성 검색**(이름·연락처, 매출 입력과 동일 패턴). 선택 시 병원명 자동 채움 유지.
- 재문의 선택 시 **유입 경로 섹션 비활성**(안내문 병기) + 광고 상세 숨김 — 재문의는 신규 유입 성과 지표(코호트·퍼널·ROAS)에서 제외되므로 경로 정보가 쓰이지 않음. 저장은 sourceCategory '기타' 고정.
- **신규 입력 병원명 ↔ 기존 고객 자동 대조**: 병원명 입력 중 유사 기존 고객 제안(부분 일치 + **초성 앞부분 일치** — "ㄱ" → 강한·김해·가산·굿윌…, 클라 매칭·목록 1회 로드). 제안 클릭 시 '기존 재문의' 자동 전환 + 고객 연결 — 기존 고객을 신규로 등록해 생기는 지표 오염·중복 기존 고객(전환은 정확 일치만 병합)을 등록 전에 차단.

## 59. 2026-08-14 정기 등록 '초기 금액 0' 버그 (굿윌 서면 28M 사고)

- **증상**: 정기 매출 등록(월 700만, 2026-08~12)이 총액 2,800만으로 계산 — 3,500만이어야 함.
- **원인**: 초기 금액란에 **0을 입력**하면 서버·프론트 모두 "혼합형 초기 회차 있음"으로 취급 → 시작월(8월)에 **0원 초기 회차**가 생기고 월 회차는 익월(9월)부터 → 시작월 월액 누락 + 총액 한 달치 과소(0 + 700만×4 = 2,800만). 미리보기에도 같은 논리라 "초기 ₩0 + 4개월"로 표시됐지만 눈에 띄지 않았음.
- **수정**: 초기 금액 0 = 미입력 동일 취급(서버 unset + 프론트 >0일 때만 전송), 미리보기 규칙 동일화 + "등록 미리보기" 라벨 명시.
- **데이터 정정**: 굿윌 서면 8월 회차 0→700만(비용 100만), 그룹 총액 2,800만→3,500만 — UI(회차 금액 편집·프로젝트 편집)로 정정.

## 60. 2026-08-14 정기 유지율 + 무기한 정기 (#54 — 주간보고 대체 2단계)

- **유지율 정의**: 월 M의 유지율 = 전월 정기 활성 클라이언트 중 M월에도 활성인 비율(클라이언트 단위·기간 기반, 신규 진입은 분모 밖 — 이탈만 반영). "활성" = 시작월 ≤ M ≤ 종료월(null=무기한), 해지 그룹 제외. **이탈 처리 = 정기 관리에서 종료월 입력**(다음 달부터 이탈로 집계).
- **정기 관리 카드**: 유지율 % + "전월 N곳 중 M곳 유지" + 이탈 클라이언트명(빨강) + 6개월 추이. `RecurringRetention` 서비스 — #57 주간 성과보고에서 재사용 예정.
- **무기한 정기(요청 반영)**: 종료월이 필수라 무기한 계약을 못 담던 문제 해소 — 종료월 미입력 시 무기한: 당월까지 회차 우선 생성 + **contracts:recurring-roll**(매일 06:05, 멱등)이 매월 당월 회차 자동 생성(항상 '진행 중' — 수금 확정은 사람, 비용은 직전 월 승계). 총액은 null('—'), 기간은 '무기한' 표시, 월액 변경 상한 해제.
- 검증(로컬 E2E): 무기한 등록→6~8월 3회차·총액 null → 8월 회차 삭제 후 roll 재생성(ongoing·비용 승계·'정기 자동 생성' 이력) → 종료월 설정 시 이탈 집계(0%·이탈명) → 원복. 브라우저 카드 렌더 확인.

## 61. 2026-08-18 매출 현황 일괄 변경 (#59) + 8월 정기 일괄 등록

- **8월 정기 일괄 등록**: 주간 성과보고 p7~9 기준 12건(통합 마케팅 9 + 검색광고·유지보수 3) 무기한 정기로 등록 — 기입력분 제외, 보고서 정기매출(예상) 24,952,400과 원 단위 일치(+기존 참좋은·동래 393,800은 보고서 밖 데이터로 보존). 미수금 5건은 과거 일감이라 미입력(#30에서 처리).
- **일괄 변경**: 매출 현황 표에 체크박스 열 + 선택 액션 바. 허용 = 청구서/결의서 체크·해제, **완료(수금) 처리**(수금일 1개 지정 다이얼로그 — 대상 건수·합계 명시, 취소·기완료·금액 미입력 자동 제외 + 사유 토스트), 휴지통(2차 확인·권한 없는 건 자동 제외). **제외 = 금액·비용·매출일·취소·수금 취소**(행마다 값·사유가 달라 개별 편집만 — 사고 방지 원칙).
- 서버 POST /contracts/bulk — 단일 트랜잭션(중간 실패 시 전체 롤백), 회차별 이력('(일괄)' 표기), 과거 월 스냅샷 자동 rebuild.
- 검증(로컬 E2E): 청구서 일괄 3건 → 완료 일괄(수금일 8/28) → 제외 사유('이미 완료') 반환 → 원복. 브라우저에서 전체 선택·액션 바·완료 다이얼로그 확인.
- **순매출 카드 지출 수정(같은 날 사용자 확인)**: 기간 밖 미수금 회차의 비용이 이번 기간 카드에서 재차감되던 문제 — 지출은 발생 월 귀속이므로 기간 내 회차만 합산(미수금 '매출' 표시는 #50대로 유지). E2E: 7월 미수금(결의서 체크·비용 33만) 생성 → 8월 카드 지출에서 제외 확인 → 원복.

## 62. 2026-08-18 매출 표시·정기 편집 3건 (사용자 요청)

- **예상 매출 → 예상 순매출**: 매출 현황 카드 메인 = Σ(금액−비용), 서브라인에 매출·비용 병기 + **비용 미입력 n건** 경고(0원으로 계산됨 — 입력 시 순매출 하락 주의). 전망 개념이라 결의서 여부와 무관하게 포함 회차의 비용을 짝지어 차감(미수금 회차 포함 — 매출이 포함되는 회차는 비용도 함께. 확정 지출 카드의 발생 월 귀속과 별개 계산).
- **지출 전용 회차 제외**: 금액 0 + 비용>0 회차(예: 동래센트럴 스마트LED배너 지출 2회)는 수금 회차 수·진행률에서 제외 — `scopeRevenueInstallments` 단일 스코프(paidSoFar 체인 + 정기 total_count). 비용은 expenseRecognized로 계속 차감. 운영 검증: 수금 3회→1회.
- **정기 정보 편집**: "왜 기간은 변경이 안 되냐" — 의도된 잠금이 아니라 미구현이었음. 월액 변경 다이얼로그에 '정기 정보' 섹션 추가: **종료월**(당김=해지 → 이후 미청구 진행 중 회차 휴지통 정리·수금/청구분 보존 / 늘림=연장 → 새 월 회차 자동 생성 / 무기한 전환), **매출일**(이후 자동 생성분부터), 서비스명·메모. **시작월만 불변**(기생성 회차 전체와 얽힘 — 잘못 등록 시 삭제 후 재등록). E2E: 무기한→12월(4회차 생성)→9월 해지(3회차 정리)→무기한 전환 검증.

## 63. 2026-08-18 정기 편집 확장 — 시작월 변경 + 다이얼로그 순서 + 동래센트럴 정정

- **동래센트럴 스마트LED배너 정정**: 월 정기(11만) 시작을 9월로 — 8/31 회차(미청구·진행 중) 휴지통 정리(마이그레이션·30일 복구 가능). 초기 회차(7월 220만)·총액 유지. (교훈 2건: contract_logs는 updated_at 없음·id 자동 증가 — DB::table 직insert 시 주의)
- **시작월 변경 기능(승인 규칙)**: 미루면 이전 미청구·진행 중 회차 휴지통 정리, **수금·청구된 회차가 걸리면 차단**(사실 기록 보호), 당기면 당겨진 구간 회차 자동 생성(진행 중 — 수금은 사람이 확정). **초기 금액(혼합형) 정기는 시작월 변경 불가**(초기 회차가 시작월에 붙어 있음 — 삭제 후 재등록 안내). 기간형은 총액 재계산.
- **다이얼로그 순서**: 정기 정보 → 월액 변경 → 월액 변경 이력 (사용자 요청).
- E2E: 미루기(미청구 1건 정리)·당기기(3회차 생성·전부 진행 중)·수금 앞 차단 검증.

## 64. 2026-08-19 건수 KPI 목표 (#55 — 주간보고 대체 3단계)

- **kpi_goals/kpi_goal_logs**(RevenueGoal 패턴 — PUT 덮어쓰기+불변 이력) + **KpiMetrics 서비스**(실적 산식 단일 소스, #57 보고서 재사용 예정).
- **실적 산식**: 신규 문의 = 유입일 당월·재문의 제외(대시보드 동일 기준) / 제안·계약 = 당월 해당 단계 **진입** 고객 수(단계 변경 이력 + 그 단계로 바로 등록, 고객 단위 중복 제거, 재문의 포함 — 기존 고객 제안·계약도 영업 활동).
- **화면**: 매출 목표에 '건수 KPI 목표' 섹션(월 선택 + 3개 입력 + 최근 변경), 성과 분석에 '건수 KPI 추이' 표(최근 3개월 목표·실적·달성률 — 주간보고 슬라이드 3 구조, 100%↑ 초록·60%↓ 주황).
- E2E: 목표 저장(15/6/6)→견적/제안 등록 시 제안·신규 +1→계약 전환 시 계약 +1→덮어쓰기 이력(6→8) 검증. ChangeLog에 customer 관계 추가(휴지통 고객 제외용).
- **달성률 카드 기간 연동(같은 날 사용자 지적)**: "비우면 올해 전체"인데 카드만 이번 달 고정이던 혼란 — 기간 끝 월이 지정되면 그 월의 **확정 달성률**(일차·월말 예측·경과선 숨김), 비우면 기존 이번 달 페이스 카드. 필터 문구에 '달성률은 끝 월 기준' 명시.

## 65. 2026-08-19 IA 개편 — 목표 관리 이동·성과 분석 재정렬 (사용자 승인)

- **목표 관리 신설**: 매출 목표+건수 KPI 통합 화면(/revenue/goals — URL 유지)을 매출 관리 → **성과 분석 그룹 끝**으로 이동. 근거: 목표는 성과를 재는 기준이라 달성률 확인처와 같은 그룹(설정→확인 동선 완결), "매출 목표만 덩그러니" 문제는 통합으로 해소. 매출 관리는 매출 현황·정기 관리 2탭(순수 입력·기록).
- **성과 분석 탭 순서**: 추이·기여도 → 온라인 유입 퍼널 → 채널별 → 캠페인별 → 목표 관리 — 온라인 성과 위주 시절의 "광고→유입" 순서를 매출 중심으로 반전.
- **추이·기여도 카드 순서**: 달성률 → **건수 KPI 추이**(주요 목표) → ROAS·수익률 → 추이+기여도 → Top 거래처 → 코호트.
- 경로 언급 문구 연쇄 갱신(연동 맵 §3): KPI 표 "목표 입력: 목표 관리 탭", 목표 화면 브레드크럼 "성과 분석", KPI 섹션 안내.
- 전수 검토 결과 그 외 그룹(고객·보고서·설정)은 원칙("입력·기록 vs 해석 vs 구성") 부합 — URL-그룹 불일치(/revenue/existing 등)는 내부 문제라 유지.

## 66. 2026-08-19 대시보드 순매출 카드 — 이중 톤 목표 막대 (A안, 사용자 아이디어)

- 확정만 보여 예상 매출을 알 수 없던 문제 — **한 막대에 확정(진한 주황)+예상 추가분(연한 주황)**, 분모 = 월 목표. "확정 0% · 예상 포함 21%"가 막대 하나로 읽힘. 메인에 '예상 ₩X' 병기, 신규/기존 구성은 텍스트 줄로.
- 서버 kpi.expectedProfit = 해당 월 회차(완료+진행 중) Σ(금액−비용) — 매출 현황 예상 순매출의 월 내 부분과 동일 산식(타월 미수금은 조치 카드 소관), 목표 미설정 시 목표 관리 안내 문구.
- **[대기 — 투두 등록 예정] 유입채널 추가·관리(08-21 사용자 요청, 계획 수립 → 승인 대기)**: 세부 유입경로를 화면에서 추가·관리(설정 메뉴) — 하드코딩(KoLabels ↔ mock-customers 이원, 연동맵 §6 🔴) 해소 포함.
- **[대기 — 투두 등록 예정] 대시보드 정리안(08-19 검토 완료, 사용자 승인 대기 아님·투두 보관 지시)**: ① 전환수 카드 제거(+150만 추정 로직 삭제 — 가정값·발동 이력 없음, 실데이터 구글 conv 292건뿐) ② 채널별 성과 비교·채널 비중 변화를 성과 분석 › 채널별 상세 상단으로 이동 ③ 수집 신뢰도 요약(TrustSummary) 제거(상단 바·조치 카드와 3중복) ④ 목표 달성률 배너(GoalProgressCard)를 순매출 카드에 흡수(전년 동월 비교만 이관) — 광고 요약 카드는 유지. 결과: 대시보드 = KPI 3장+조치 3장+추이.

## 67. 2026-08-21 클라이언트 이슈 로그 (#56 — 주간보고 대체 4단계)

- **client_notes**: 기존 고객별 시간순 이슈·메모(불변 — 수정 없음, 삭제는 작성자·관리자만). 고정 메모(memo)와 역할 구분: 메모=상시 특이사항 1칸, 이슈 로그=주 단위로 쌓이는 타임라인.
- **화면**: 기존 고객 상세에 '이슈·메모' 카드(입력 + Enter 등록 + 최신순 50건 + 삭제), 기존 고객 목록의 병원명 아래 **최근 이슈 1건 병기**(보라색·60자) — "요즘 무슨 일 있는 곳"이 목록에서 보임.
- **용도**: 주간 성과보고 슬라이드의 클라이언트별 '주요 이슈사항' 소스 — #57이 이번 주 작성분을 자동 취합. 평소 미팅·제안·특이사항을 한 줄씩 남기면 보고서가 모아줌.
- E2E: 등록 2건→상세 최신순→목록 latestNote→삭제→원복. 운영 마이그레이션 DONE·전체 재빌드.
