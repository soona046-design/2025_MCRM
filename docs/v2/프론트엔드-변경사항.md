# M-CRM v2 — 프론트엔드 구현 변경사항 (2026-07-11)

> 대상: `mcrm-v2/` (Next.js, 목데이터 기반)
> 근거: [기능명세서.md](기능명세서.md) · [IA-화면목록.md](IA-화면목록.md) — 2026-07-11 manyfast PRD 갱신분 반영
> 상태: **검증 완료 · 커밋됨** (2026-07-11). 빌드·타입체크 통과 + 브라우저 실클릭 검증 완료 — 매출 직접 입력(신규 유입 검색연결→등록→목록 반영), 기존 고객 신규 등록 중복 경고, 단계 역행 사유 필수(이력 기록), URL 붙여넣기 UTM/클릭ID 자동 파싱(확정 귀속 배지 전환), 채널→캠페인 드롭다운 연동, 업로드 5단계·금액 컬럼 제거, 기존 고객 목록/상세, 목표 스코프 탭, 분석 이중축·기여도 안내, 대시보드 "지금 수집"(실패→지연 전환)까지 전부 동작 확인. 콘솔 에러 0건.
> **2026-07-12 폴리싱**: 경미 관찰 2건 모두 원인 파악 후 수정 완료 — 상세는 §6 참고.
> **2026-07-12 스펙 정합화**: 명세서 자체의 내부 모순(초반 요약표 vs §1.3.8 상세 스펙) 발견·해소. 메인
> 대시보드 KPI 카드에 누락돼 있던 ROI(%) 수치를 추가하고, 대시보드 ROAS 차트를 상세 스펙이 요구하는
> 단일축(+광고비 배경 막대)으로 되돌렸으며, 이중축 ROAS·ROI 비교 차트는 §7.3.1이 지정한 제자리
> (`/revenue/analytics`)에만 남기고 그 화면에 빠져있던 광고비 막대·인터랙티브 툴팁을 채웠다.
> 상세는 §7 참고.
> **2026-07-12 대시보드 인사이트 강화**: 피그마 유저플로우 대조에서 채택 가치가 확인된 3건 구현 — 퍼널
> 이탈 집중 구간 자동 하이라이트, 대시보드 상단 목표 달성률 요약 카드, ROAS·전체 매출 카드의 전년
> 동기(YoY) 비교 배지. 작업 중 발견한 ROI 배지 오류(당월 부분 실적 ÷ 월 전체 광고비 → -335.7%p)도
> 광고비 pace 보정으로 수정. 상세는 §8 참고.

---

## 1. 핵심 변경 요약

| 변경 | 이전 | 이후 |
|---|---|---|
| 퍼널 단계 | 문의 → 상담 → 예약 → 계약 (4단계) | 유입 이벤트 → 리드 → 상담 → 견적/제안 → 계약 (5단계) |
| 계약 금액 입력 위치 | 고객 상세 화면(유입 관리) | **매출 관리 전용** — 유입 관리에서는 입력 UI 자체가 없음 |
| 매출 지표 | ROAS 단일 (계약 금액 전체 ÷ 광고비) | **ROAS**(신규 유입 고객 매출만) / **ROI**(신규+기존 전체 매출) 분리, 라벨 상시 병기 |
| 고객 구분 | 없음 | **신규 유입 고객**(유입 관리 연결) vs **기존 고객**(재계약·직거래) |
| 광고 매칭 증거 입력 | UTM/클릭ID 수기 입력만 | + 캠페인 드롭다운 자동연동, URL 붙여넣기 자동파싱 |
| 수집 모드 표현 | (미정) | 정기 수집(06:00) / **수동 재수집**("지금 수집" 버튼) 2모드 |
| 화면 수 | 20개 | **21개** — 기존 고객 목록/상세(D5) 신규 |

---

## 2. 신규 파일

| 파일 | 내용 |
|---|---|
| `src/lib/mock-existing-customers.ts` | 기존 고객 목데이터 스토어. 검색(`searchExisting`), 중복 후보 탐지(`findDuplicateCandidate`), 신규 등록(`addExistingCustomer`) |
| `src/lib/utm.ts` | `parseUtmUrl()` — 붙여넣은 URL/쿼리스트링에서 utm_source/medium/campaign, 클릭ID(gclid/gbraid/wbraid/nclk) 자동 추출 |
| `src/components/revenue/contract-ui.tsx` | `ContractStatusBadge`, `CustomerTypeBadge`, `EditAmountDialog`(금액 인라인 편집 — ROAS/ROI 재계산 안내) — 계약 목록·기존고객 상세 화면 공용 |
| `src/app/(main)/revenue/existing/page.tsx` | **D5** 기존 고객 목록 — 검색/담당자 필터, 마지막 계약일·누적 매출액 정렬 |
| `src/app/(main)/revenue/existing/[id]/page.tsx` | **D5** 기존 고객 상세 — 누적 매출 요약, 다중 계약 이력 표, 재계약 주기 안내 |

## 3. 수정 파일

### 데이터 레이어
- **`src/lib/mock-customers.ts`** — `Stage` 타입을 5단계로 교체, `Customer`/`NewCustomerInput`에서 `contractAmount` 제거, 광고 채널·캠페인 드롭다운 데이터(`AD_CHANNELS`, `CAMPAIGNS_BY_CHANNEL`) 추가
- **`src/lib/mock-revenue.ts`** — 전면 재작성. `contracts`가 계약 금액을 단독 소유(고객 스토어에서 파생하지 않음), `customerType`(신규 유입/기존) 필드, `addContract`/`updateContract`(이력 기록), `roasRoiTrend()`(이중축 추이), `GOAL_SCOPES`(고객유형별 목표)

### 고객 유입 관리
- **`customers/[id]/page.tsx`** — 계약 금액 카드·다이얼로그 제거 → "매출 관리에서 계약 금액 등록" 링크로 대체. 단계 역행 시 사유 선택(재검토 필요/오류 수정/기타) 필수화
- **`customers/new/page.tsx`** — 계약 금액 필드 제거. 광고 채널 선택 시 캠페인 드롭다운 자동 연동(목록에 없으면 직접 입력 전환), "URL 붙여넣기" 입력란으로 UTM/클릭ID 자동 파싱
- **`customers/upload/page.tsx`** — `STAGE_ALLOWED` 5단계로, `contractAmount` 컬럼·검증·템플릿/샘플 CSV에서 전부 제거

### 매출 관리
- **`revenue/page.tsx`(D1)** — 고객유형 탭(전체/신규 유입/기존), "매출 직접 입력" 다이얼로그(신규=유입 고객 검색연결, 기존=자동완성 검색 또는 신규 등록+중복 병합 확인), 금액 클릭 시 인라인 편집, 목록에 고객유형 컬럼 추가
- **`revenue/analytics/page.tsx`(D4)** — 고객유형 필터 탭, ROAS(좌축)·ROI(우축) 이중축 차트 추가, 채널 기여도는 신규 유입 전용(기존 선택 시 안내 문구)
- **`revenue/goals/page.tsx`(D3)** — 목표를 전체/신규 유입/기존 탭으로 세분화, 이력에 구분 배지 표시
- **`components/revenue/revenue-tabs.tsx`** — "기존 고객 목록" 탭 추가

### 대시보드(B1)
- **`components/dashboard/kpi-cards.tsx`** — "계약 금액" 카드 → "전체 매출"(전체 포함 라벨)로 교체, ROAS 카드에 "신규 유입 기준" 라벨 추가
- **`components/dashboard/roas-trend.tsx`** — 채널별 ROAS 추이 → **ROAS·ROI 이중축 추이**로 전면 교체(`roasRoiTrend()` 사용, 실제 계산값 반영)
- **`components/dashboard/funnel-chart.tsx`** — 5단계(유입 이벤트→리드→상담→견적/제안→계약)로 갱신
- **`app/(main)/page.tsx`** — 레이아웃을 명세 순서(요약카드 → ROAS·ROI 추이 → 채널비교+퍼널 → 채널비중 → 신뢰도)로 재배치, "지금 수집"(수동 재수집) 버튼 추가

### 내비게이션
- **`components/layout/app-shell.tsx`** — 사이드바 "매출 관리" 하위에 "기존 고객 목록" 추가, 상세 페이지 활성 상태 매칭 규칙 보강

---

## 4. 반영하지 않은 항목 (범위 밖)

- **백엔드**: v2 Laravel 신규 프로젝트는 아직 미착수(VPS 등 인프라 결정 필요, 별도 확인 후 진행)
- **B2/B3(채널·캠페인 상세 분석), E1~E4(보고서), F1~F5(설정)**: 기존에도 목데이터 스캐폴드가 없던 화면이라 이번 범위에서 신규 구축하지 않음 — 사이드바에 "준비 중"으로 표시된 채 유지
- **브라우저 인터랙션 테스트**: 이 세션에서 Claude in Chrome 확장이 연결되지 않아 폼 제출·다이얼로그 클릭 등 실제 조작 확인은 못 했음. curl 기반 SSR 콘텐츠 검증(라우트 200, 신규 문구 노출)까지만 완료

## 5. 확인 방법

```bash
cd mcrm-v2
npm run dev   # http://localhost:3000
```

주요 확인 포인트:
- `/customers/new` — 광고 채널 선택 → 캠페인 드롭다운, URL 붙여넣기 자동파싱
- `/customers/[id]` (계약 단계 고객) — 계약 금액 입력란이 없고 매출 관리 링크만 노출
- `/revenue` — "매출 직접 입력" 다이얼로그, 고객유형 탭, 금액 인라인 편집
- `/revenue/existing`, `/revenue/existing/[id]` — 신규 화면
- `/` — KPI 카드 "전체 매출"(ROI% + 전월 대비 배지 포함), ROAS 단일축 추이(+광고비 배경 막대), "지금 수집" 버튼
- `/revenue/analytics` — ROAS·ROI 이중축 차트(광고비 배경 막대 + 포인트 클릭 시 ROAS/ROI/매출/광고비 툴팁)

---

## 6. 2026-07-12 폴리싱 — 경미 관찰 2건 조치

### ① 기존 고객 상세 "재계약 주기 안내" 일부 미노출
- **원인**: `revenue/existing/[id]/page.tsx`가 "오늘" 기준으로 `new Date()`(실제 시스템 시각)를 썼는데, 목데이터의 계약일은 전부 2026년으로 고정돼 있어 실제 시계와 어긋나면 경과일 계산이 항상 0 또는 비정상적으로 커짐 — 다른 화면(`mock-customers.ts`의 `NOW_REF`, `customers/page.tsx`의 `NOW`)은 전부 앱 내부 고정 기준시각을 쓰는 것과 불일치했던 게 근본 원인.
- **조치**: `mock-revenue.ts`가 이미 내보내는 `CURRENT_MONTH`(`2026-07`)·`DAYS_ELAPSED`(`10`)로 고정 기준일(`2026-07-10`)을 구성해 사용하도록 수정. 부수적으로 `daysBetween`의 불필요한 `Math.max(0, …)` 클램프도 제거(미래 날짜 계약과의 차이를 자연스럽게 음수로 표현해 조건이 올바르게 `false`가 되도록).
- **부가 조치**: 이 기준일로 재계산하면 목데이터 생성 규칙상 최근 계약이 항상 "충분히 최근"이라 배너가 사실상 아무에게도 안 뜨는 2차 문제가 드러나, `mock-revenue.ts`의 기존 고객 계약 시드에서 계약 2건 이상 고객의 절반(`ei % 2 === 0`)은 마지막 계약을 의도적으로 3개월 더 이전으로 배치하도록 조정. 계약 1건 고객은 애초에 주기를 계산할 수 없어 항상 미노출인 것이 정상 동작.
- **검증**: `ex1000~ex1011` 12명 중 `ex1002·ex1004·ex1008·ex1010`(계약 2건 이상 + 오래된 마지막 계약)에서만 배너 노출 확인, 나머지는 미노출 — 데이터 조건부 동작임을 확인.

### ② ROAS·ROI 차트 당월 라벨 겹침
- **원인**: 당월(7월) ROAS가 급락(0.45배)하고 ROI가 마이너스(-17.8%)로 나오면서 두 값의 화면상 y좌표가 서로 가까워졌는데, 라벨 위치를 각 포인트 기준 고정 오프셋(`-8`/`+16` 등)으로만 계산해 겹침 방지 로직이 없었음.
- **조치**: `components/dashboard/roas-trend.tsx`와 `revenue/analytics/page.tsx`의 `RoasRoiChart` 두 곳 모두에 최소 간격(14~16px) 보장 로직을 추가 — 두 라벨이 너무 가까우면 아래쪽(ROI) 라벨을 밀어내고, 캔버스 하단을 벗어날 경우엔 위쪽(ROAS) 라벨을 대신 밀어 올리는 방식으로 항상 최소 간격과 캔버스 내부 범위를 보장.
- **검증**: 렌더링된 SVG에서 두 라벨의 실제 `y` 좌표 차이가 대시보드 43px, 분석 화면 50px로 벌어져 있음을 확인(둘 다 `MIN_GAP` 이상, 캔버스 범위 내).

## 7. 2026-07-12 스펙 정합화 — ROAS·ROI 대시보드 배치 오류 수정

### 배경: 명세서 자체의 내부 모순
- **초반 요약표** (§1, 개요 수준 표): "2행: ROAS·ROI 기간별 추이 — 이중축 라인 차트"
- **§1.3.8 상세 스펙** (더 구체적, 더 나중에 작성됨): 1행 카드에 "전체 매출(ROI 기준)" 카드를 별도 배치하고,
  2행은 **ROAS 단일축 차트 + 광고비 배경 막대**로 명시. 그리고 못박음: "ℹ️ ROI(전체 매출 기준)는 1행
  요약 카드에서 확인하세요. 보고서의 ROAS·ROI 비교 차트에서 두 지표를 함께 볼 수 있습니다."
- **§7.3.1** (매출 관리 › 추이·기여도 분석, `/revenue/analytics`): 이중축 ROAS·ROI 비교 차트의 정식 스펙
  (좌 ROAS·우 ROI, 광고비 배경 막대, 클릭 시 4값 툴팁, 데이터 누락 회색 점선)이 여기 있음

기존 구현은 초반 요약표만 따라가 이중축 차트를 메인 대시보드에 넣었고, §1.3.8이 요구한 "전체 매출 카드의
ROI(%) 표시"는 누락돼 있었음. 상세 스펙이 배치를 명확히 이원화하고 있어 §1.3.8/§7.3.1을 따르는 쪽으로
정합화(사용자 확인 후 진행).

### 조치
- **`kpi-cards.tsx`**: "전체 매출" 카드에 ROI(%) 수치 + 전월 대비 증감 배지(%p) 추가, 라벨을 "전체 매출
  기준"으로 고정 표기 (ROAS 카드와 분자가 다름을 항상 구분)
- **`mock-revenue.ts`**: `roasRoiTrend()`가 `adCost`·`newLeadRevenue`·`totalRevenue`도 함께 반환하도록
  확장, 전월 대비 ROI 증감을 계산하는 `roiMoMDelta()` 신규
- **`components/dashboard/roas-trend.tsx`**: 이중축 → **ROAS 단일축 + 광고비 배경 막대**로 교체. 제목
  "ROAS 기간별 추이 (신규 유입 고객 기준)"
- **`revenue/analytics/page.tsx`의 `RoasRoiChart`**: 이중축 차트를 §7.3.1 요구사항에 맞춰 보강 —
  광고비 배경 막대 추가, 포인트 클릭 시 ROAS/ROI/매출/광고비 4값을 동시에 보여주는 인터랙티브 툴팁 패널
  추가(hover + click 모두 지원)
- **미반영 항목**: §7.3.1의 "데이터 누락 구간 회색 점선 표시"는 목데이터에 결측 월이 없어 실제 렌더링
  검증은 하지 못함 — 실 데이터 연동(M3) 시 확인 필요

### 검증
- 클린 빌드(`rm -rf .next && next build`) + `tsc --noEmit` 통과
- 브라우저 실클릭 검증: 대시보드 KPI "전체 매출" 카드에 "ROI -17.8%" + "▼ 335.7%p" 배지 노출 확인,
  ROAS 단일축 차트에 광고비 배경 막대 렌더링 확인, `/revenue/analytics`에서 1월 포인트 클릭 →
  툴팁 패널이 "1월 · ROAS 2.50배 · ROI 250.0% · 매출 ₩24.5M · 광고비 ₩7.0M"으로 즉시 갱신 확인
- 콘솔 에러 0건(새 브라우저 탭 기준 — 기존 탭에는 여러 차례 코드 수정을 거치며 쌓인 stale HMR 번들
  경고가 남아있었으나 실제 버그 아님, 새 탭·클린 리로드로 재현 안 됨을 확인)

## 8. 2026-07-12 대시보드 인사이트 강화 — 피그마 대조 채택 3건 + ROI 배지 버그 수정

### 배경
피그마 유저플로우(2026-07-11 작성분)를 현재 구현·명세서와 3자 대조한 결과, 피그마 플로우 자체는
7/11 PRD 갱신 **이전** 규격(4단계 퍼널, ROI 미분리 등)이라 대부분 무시했으나, 피그마에만 있고 구현에는
없던 **인사이트 표현 3건**은 명세(§1.2.2 성과 저하 인사이트, §7.2.2 달성률, §7.3 전년 동기 증감)와
합치해 채택했다.

### 조치 (신규 파일 1 + 수정 3)
- **`components/dashboard/funnel-chart.tsx`** — 각 단계에 전월 대비 전환율 증감(%p) 데이터를 부여하고,
  가장 크게 하락한 전환 구간을 자동 감지해 ① 해당 전환율 수치를 빨간색으로, ② 카드 하단에 "🔴 이탈 집중
  구간: A→B 전환율 N% — 전월 대비 -N%p 하락" 경고 배너로 표기. (worstIdx는 하드코딩이 아니라 배열에서
  계산 — 데이터 교체 시 자동 이동)
- **`components/dashboard/goal-progress-card.tsx`** (신규) — 대시보드 최상단(KPI 카드 위) 목표 달성률
  요약. 이번 달 매출/목표/달성률·월 경과 세로선이 있는 진행 막대·월말 예측(미달 시 "미달 우려" 빨간
  표기). 전체 스코프 기준이며, 스코프별 상세는 매출 관리 › 추이·기여도 분석으로 위임.
- **`components/dashboard/kpi-cards.tsx`** — ROAS 카드와 전체 매출 카드에 "전년 동기 대비" 배지 추가.
  ROAS 헤드라인 수치·전월 대비 배지를 정적 목업값(3.2배/+0.4)에서 실제 계산값(`roasRoiTrend()`/
  `roasMoMDelta()`)으로 연결해 다른 지표와 정합.
- **`lib/mock-revenue.ts`** — `yoyDelta()`(전년 동월 시드 대비 매출·ROAS 증감, pace 보정), `roasMoMDelta()`
  신규.

### ROI 배지 버그 수정 (§7에서 노출된 -335.7%p의 실제 원인)
§7 검증 당시 "전체 매출" 카드 전월 대비 배지가 **-335.7%p**로 찍혔는데, 이는 당월(10일차) **부분 실적**을
**월 전체 광고비**로 나눠 ROI가 비정상적으로 낮게(-17.8%) 계산된 결과였다. `roasRoiTrend()`의 당월 행이
광고비를 경과일 비율(`DAYS_ELAPSED/DAYS_IN_MONTH`)만큼 환산(pace 보정)하도록 고쳐, ROAS·ROI 모두
"현재 속도" 기준으로 앞선 달들과 같은 선상에서 비교되게 했다. 이제 대시보드와 `/revenue/analytics`가
동일 값(ROAS 1.40배 / ROI 154.8%)을 표시한다.

### 검증
- `tsc --noEmit` 통과
- 브라우저 실측(1440px): 목표 달성률 카드 "19.6% · ≈₩25.5M 미달 우려", ROAS 카드 "1.40배 ▼-1.64 ·
  전년 동기 대비 -0.8배(-37.2%)", 전체 매출 카드 "ROI 154.8% · 전년 동기 대비 +20.2%", 퍼널 하단
  "🔴 이탈 집중 구간: 상담→견적/제안 전환율 46.6% — 전월 대비 -8.2%p 하락" 노출 확인. 콘솔 에러 0건.

## 9. 2026-07-12 B2·B3 채널/캠페인 상세 분석 화면 (M3 프론트)

대시보드 하위 2개 화면을 목데이터로 신규 구현. 사이드바에서 "준비 중"(href 없음)이던 자리를 실제
라우트로 연결하고, 3화면 간 이동 탭(`DashboardTabs`)을 통합 요약 화면에도 추가했다.

### 신규 파일
- **`lib/mock-analytics.ts`** — 채널×월 지표(노출·클릭·광고비·전환·확정귀속/소스미상 매출·완결도) 생성기와
  캠페인·키워드 시드. ROAS 분자는 확정 귀속 매출만. 당월은 경과율(0.32) 반영한 부분 실적.
- **`app/(main)/dashboards/channels/page.tsx`** (B2, §1.2·§1.3.6, S-RJQSXZ) — 기간 필터(이번 달/최근
  3·6개월), 채널×기간 검증용 상세표(확정귀속·소스미상 분리, 합계행), 메타 수집 실패 시 회색 행·합계
  제외, 채널별 월간 광고비 스택 막대(표+그래프 병행), "이 조건으로 보고서 만들기" CTA(E2 준비 중이라
  현재는 필터 인계 toast).
- **`app/(main)/dashboards/campaigns/page.tsx`** (B3, §1.2.1·§1.3.2, S-XIEUKQ) — 캠페인 ROAS 순위표,
  입력률·UTM 매칭률·귀속률 병기(85%↓ 경고색), 검색광고 캠페인 행 클릭 시 키워드 효율(클릭/CPC/전환율)
  펼침. 메타·디스플레이는 키워드 데이터 없음 안내.
- **`components/dashboard/dashboard-tabs.tsx`** — 통합 요약/채널별 상세/캠페인별 상세 이동 탭(RevenueTabs 패턴).

### 수정 파일
- **`components/layout/app-shell.tsx`** — 대시보드 하위 메뉴를 "채널 리포트/전환 분석"(placeholder)에서
  "채널별 상세/캠페인별 상세"(실 라우트)로 교체.
- **`app/(main)/page.tsx`** — 상단에 `DashboardTabs` 추가.

### 검증
- `tsc --noEmit` 통과, 콘솔 에러 0건
- 브라우저 실측: B2 최근 3개월 합계 "광고비 ₩27.37M(메타 제외) · ROAS 3.07배", 메타 행 "수집 실패·
  ROAS —" 확인. B3 ROAS 내림차순(4.00→2.00배), 메타 캠페인 매칭·귀속률 경고색(71/58%, 68/52%),
  "7월_임플란트_브랜드" 행 클릭 시 키워드 3개(임플란트 가격 등) 펼침 확인.
- 미반영: 보고서 CTA는 E2(보고서 생성) 화면이 아직 없어 toast로 대체 — M5에서 필터 자동 인계(S-LMUKBI) 연결 예정.
  → **§10에서 해소**: E2 구현과 함께 CTA를 `/reports/new?from=…&channels=…` 라우팅으로 교체.

## 10. 2026-07-12 M5 보고서 화면 E1~E4 (목록·생성·뷰어·버전 비교)

보고서 4화면을 목데이터로 신규 구현. 사이드바 "준비 중"이던 보고서 메뉴를 실 라우트로 연결하고,
B2·B3의 "이 조건으로 보고서 만들기" CTA를 보고서 생성 화면 필터 인계로 교체(§9 미반영 항목 해소).

### 신규 파일
- **`lib/mock-reports.ts`** — 버전 시드(실패 버전 1건 포함), 생성 이력·저장 필터를 모듈 스코프 배열로
  유지. `generateReport`는 최신 요약값에서 소폭 변동한 새 버전을 append. `saveFilter`(최대 10개, 이름 중복 차단).
- **`components/reports/report-tabs.tsx`** — 보고서 목록/생성 이동 탭.
- **`app/(main)/reports/page.tsx`** (E1, §3.3.3) — 버전/종류/조회기간/필터/생성자/생성일시 목록, 실패 버전
  제외, "데이터 업데이트됨" 배지, 행 클릭 → 뷰어, 비교 링크.
- **`app/(main)/reports/new/page.tsx`** (E2, §3.2·§3.3.1) — 종류(주/월/연)·채널·캠페인·지표 필터, 저장
  필터 목록·저장 다이얼로그, 대시보드 인계 배지(`?from=`)+조건 초기화, 메타 실패 시 누락 경고(포함/제외
  선택), 생성 스피너 "보고서를 만들고 있어요" → 완료 시 미리보기/PDF/Excel. `useSearchParams`는 Suspense로 래핑.
- **`app/(main)/reports/[id]/page.tsx`** (E3, §3.1·§3.3.2) — 종류별 템플릿 섹션 렌더(주간: 채널막대→퍼널
  →ROAS·전체매출 카드 / 월간: KPI 4종→채널상세·지출도넛→캠페인ROAS→퍼널 / 연간: ROAS·ROI요약→채널비중),
  신뢰도 상태바·주석, 사내 보고 고지 문구 고정, PDF/Excel 버튼(현재는 toast).
- **`app/(main)/reports/compare/page.tsx`** (E4, §3.3.3) — 최신 vs 선택 버전, 주요 지표(ROAS·광고비·계약
  금액) 증감 하이라이트(증가 파랑↑/감소 빨강↓, 광고비는 중립), 비교 대상 드롭다운.

### 수정 파일
- **`components/layout/app-shell.tsx`** — 보고서 하위 메뉴를 실 라우트로 연결, `childActive`에 뷰어·비교
  경로(`/reports/[id]`·`/reports/compare`)를 목록 메뉴 귀속으로 추가.
- **`app/(main)/dashboards/channels/page.tsx`·`campaigns/page.tsx`** — CTA를 toast → `/reports/new?from=…&channels=…` 라우팅으로 교체.

### 검증
- `tsc --noEmit` 통과, 콘솔 에러 0건
- 브라우저 실측: E1 목록 4건(실패 v3 제외, v6 "데이터 업데이트됨" 배지). E2 대시보드 인계 배지
  "네이버 검색, 구글" 채널 인계+캠페인 필터 정합, 생성 idle→"보고서를 만들고 있어요"→"보고서가
  준비되었어요" 상태 전이 확인. E3 월간 뷰어 전 섹션(KPI/채널막대/도넛/캠페인ROAS/퍼널)+사내 고지 렌더.
  E4 v4↔v6 비교 ROAS +5.8%·계약금액 +6.7% 파란 증가, 광고비 중립, 주요 지표 태그 확인.
- 한계: 생성한 새 버전(v7)은 hard reload 시 모듈 상태 초기화로 사라짐(기존 목스토어 공통 동작). SPA 내
  Link 이동에서는 유지. PDF/Excel 실제 내보내기·역할별 마스킹은 백엔드 연동(M3+) 대상.

## 11. 2026-07-12 M6 — 온보딩 A2~A4 + 설정 F1~F5 + 알림 센터

1차 출시 22개 화면 중 마지막 마일스톤(M6). 사이드바 "준비 중"이던 설정 메뉴 전체를 실 라우트로
연결하고, 헤더 알림벨을 알림 센터와 실시간 동기화. 이 세션은 별도 창(데스크톱 앱)에서 M1~M5가
진행되는 동안 병행 작업 — 겹치지 않는 M6 범위만 담당.

### 신규 파일
- **`lib/mock-settings.ts`** — 채널 연동 상태(토큰 만료 D-5 시나리오)·실패 이력 90일·외부 매출 연동
  설정, 역할별 기본 권한 매트릭스(§2.1.4)+아이디별 오버라이드(`effectivePermission`), 개인정보 보관
  정책·주간 처리 리포트, 임계치(채널 실패 20%·입력률 90/70%·목표 70%·매칭 관대/엄격), 알림 5건 시드,
  온보딩 상태(모듈 스코프). 알림 읽음 상태는 `subscribeNotifications`/`getUnreadNotificationCount`
  최소 pub-sub로 외부 공개 — 헤더 배지가 `useSyncExternalStore`로 구독.
- **`app/(main)/onboarding/page.tsx`** (A2~A4, §6) — 3단계 마법사. 1단계 채널별 연결 시뮬레이션(0.9s
  스피너)+연결 방법 다이얼로그+건너뛰기 확인("미설정 채널 N개"), 2단계 템플릿 다운로드(csv-import 재사용)
  +테스트 업로드, 3단계 체크리스트 3항목+대시보드 활성화 판정(전체/부분/비활성).
- **`app/(main)/settings/channels/page.tsx`** (F1, §1.1.1·§1.3.7) — 채널 카드(연결됨/실패/토큰 만료
  임박 D-7 경고+재연결), 실패 이력 표(미해결/복구됨), 외부 매출 연동(웹훅/API 폴링/파일, fallback 선택).
- **`app/(main)/settings/users/page.tsx`** (F2, §2.1.4~5) — 사용자 목록·역할 드롭다운·개별 설정 배지,
  기능별 허용/차단/기본값 3상 토글 다이얼로그, 변경 시 불변 이력 자동 append.
- **`app/(main)/settings/privacy/page.tsx`** (F3, §2.1.6) — 데이터 종류별 보관 개월 인라인 편집·만료 시
  처리(익명화/삭제) 배지, 주간 처리 리포트(예정/완료/실패/재시도)+실패 건 재시도.
- **`app/(main)/settings/system/page.tsx`** (F4) — 임계치 4종 편집+매칭 키 엄격/관대 모드 토글.
- **`app/(main)/notifications/page.tsx`** (F5) — 카테고리 배지(수집 실패/입력 지연/목표 미달/소급 반영/
  토큰 만료)별 색상, 카테고리→관련 화면 딥링크(채널 연동/미입력 현황/추이 분석), 모두 읽음.
- **`components/dashboard/onboarding-banner.tsx`** (§6.3 수용 기준 3) — 체크리스트 미완료 시 대시보드
  상단 경고 배너("지금 설정 완료하기" CTA), 부분 활성화 안내 문구, 완료 시 자동 숨김.

### 수정 파일
- **`components/layout/app-shell.tsx`** — 설정 하위 5개 메뉴 실 라우트 연결(광고 채널 연동/사용자 권한/
  개인정보 정책/시스템 설정/온보딩 재시작), 헤더 알림벨 button→Link(`/notifications`), 하드코딩 배지
  "2" → `useSyncExternalStore` 실시간 미읽음 수(0이면 숨김).
- **`app/(main)/page.tsx`** — `OnboardingBanner` 삽입 (메타 실패 배너 위).

### 검증
- `tsc --noEmit` 통과, 콘솔 에러 0건
- 브라우저 실측: 온보딩 1→2→3 전체 플로우(네이버 연결 "연결됨 ✔"+카운터 1/3, 테스트 업로드 "5건 성공
  확인", 체크리스트 3항목 완료 시 "전체 활성화"). 대시보드 초기 상태에서 온보딩 배너 노출 → 완료 후
  재진입 시 배너 소멸 확인. F2 권한 다이얼로그에서 "스프레드시트 업로드 차단" 클릭 → 목록 "2건 개별
  설정" 배지·변경 이력 즉시 반영. 알림 센터 "모두 읽음" → 헤더 벨 배지 실시간 소멸(수정 전엔 미동작
  버그 — pub-sub 도입으로 해결).
- 한계: OAuth 연결·토큰 검증은 시뮬레이션(M3 백엔드 연동 대상). 온보딩 상태는 모듈 스코프라 hard
  reload 시 초기화(기존 목스토어 공통 동작). A1 로그인 화면은 인증 방식 결정 후 별도 작업.

## 12. 2026-07-12 M3/B1 — 백엔드 신설 + 고객 유입 관리(C1~C5) 실 API 전환

**새로고침하면 데이터가 사라지는 문제 해소** — 목스토어(브라우저 메모리)를 실제 DB(SQLite) 기반
Laravel API로 교체. 배포 환경 결정은 보류(로컬 개발 먼저), 설계 근거는 `백엔드-설계.md`.

### 백엔드 (신규 저장소 `mcrm-v2-backend/`, 중첩 git)
- Laravel 13 + SQLite(파일 영속, 데몬 불필요 — 배포 시 MySQL 전환 가능), KST 타임존.
- 스키마(설계 §3.1·§3.3): `users`(uuid+role), `customers`(5단계 stage·stage_order), `attribution_evidences`
  (captured_via), `change_logs`(불변 — 모델 update/delete 예외 발생), `inflow_events`(입력률 KPI 분모).
- API(설계 §4 계약): 목록(q·stage·source·owner·sort·page + 단계 칩 카운트)/상세(이력 포함)/수기 입력
  (금액 필드 없음, 증거 유무→confirmed/unknown 판정)/단계 변경(역행 사유 이력)/담당자 변경/
  `GET /overdue`+`/overdue/metrics`(최근 30일 입력률).
- 저장은 영문 enum, 응답은 한글 라벨(`app/Support/KoLabels.php` 단일 변환 소스) — 프론트 화면 코드
  거의 무변경으로 전환.
- 시더가 목스토어 생성 규칙을 그대로 이식(고객 47·지연 8·사용자 4) → 전환 전후 화면 동일.
- 실행: `cd mcrm-v2-backend && php artisan serve --port 8100` (launch.json 등록됨).
  초기화: `php artisan migrate:fresh --seed`.

### 프론트 (`mcrm-v2`)
- **`lib/customers-api.ts`** 신규 — mock-customers와 동일 타입 재수출 + fetch 클라이언트
  (기본 http://localhost:8100/api, `NEXT_PUBLIC_API_URL`로 재정의).
- C1 목록: 서버 필터·정렬·페이지네이션, 검색 250ms 디바운스, 로딩/서버 다운 안내 행.
- C2 상세: 서버 응답으로 화면 갱신 — 단계 역행(사유 포함)·담당자 변경이 불변 이력으로 기록.
- C3 수기 입력·C4 업로드: `POST /api/customers`(업로드는 행 단위 순차 + 실제 진행률, 실패 건수 토스트).
- C5 미입력 현황: 서버 집계(입력률·30일 분모·지연 건수).

### 검증 (브라우저 실측)
- `tsc --noEmit` 통과, 콘솔 에러 0건.
- 수기 입력 등록(POST 201) → 목록 반영 → **하드 리로드 후 유지** 확인(핵심 수용 기준).
- 역행 변경(계약→상담, 사유 '재검토 필요')이 서버 시각·행위자(김민준·팀장)로 이력 기록.
- curl: 목록/등록/역행/담당자/미입력 목록·메트릭(92.7%) 전 엔드포인트 정상.
- 한계: 인증 없음(A1 확정 전 공개 라우트, 행위자는 시드 팀장 고정). 업로드 배치 커밋 API·대시보드/
  매출/보고서 실데이터 전환은 B2~B4. 광고 API 커넥터(B3)는 **네이버/메타/구글 API 심사 선신청 필요**.

## 13. 2026-07-12 M3/B2 — 매출·목표·분석 실 API 전환

고객 유입(§12)에 이어 매출 관리(D1~D5)를 DB 기반으로 전환. 계약·목표 데이터가 새로고침 후에도
유지되며, 달성률·ROAS/ROI·기여도는 서버가 계산한다.

### 백엔드 (`mcrm-v2-backend`)
- 스키마(설계 §3.5): `existing_customers`(유입 미연결 재계약·직거래, F-NTYUDC), `contracts`
  (new_lead/existing 분리, amount NULL=미입력 — 0으로 채우지 않음 §1.3.5), `contract_logs`·
  `goal_logs`(불변), `revenue_goals`(월×scope UNIQUE), `monthly_metrics`(과거 월 매출 스냅샷 +
  수기 광고비 — **B3 광고 API 커넥터 전 임시**).
- API: `GET/POST/PATCH /contracts`(직접 입력 시 유입 미연결 고객은 existing_customers 자동 등록,
  수정은 변경분만 불변 이력), `GET /customers/existing`+상세·자동완성(S-MYOVII)·중복 후보(S-MIQJYM),
  `GET/PUT /goals`(덮어쓰기+이력), `GET /revenue/analytics`(당월 실적 실시간 집계·scope별 추이·
  ROAS/ROI 이중축·채널 기여도).
- 계산 규칙: ROAS 분자=신규 유입 계약금액만, ROI 분자=전체 매출(S-BOZSZP·S-SVKWCD). 당월 광고비는
  경과일 비율로 페이스 환산(§7.3.1). 분모 결측 시 `null` 반환(0배 금지). 당월/경과일은 실제 시스템
  날짜 기준(목데이터의 고정 7/10 대체).
- 라우트 순서 주의: `/customers/existing*`을 `/customers/{customer}` 와일드카드보다 먼저 선언.

### 프론트 (`mcrm-v2`)
- **`lib/revenue-api.ts`** 신규 — mock-revenue와 동일 타입 재수출 + 계약/기존 고객/목표/분석 클라이언트.
- D1 계약 현황: 서버 목록(+임베디드 lead/existing 고객), 직접 입력 다이얼로그의 유입 고객 검색·기존
  고객 자동완성·중복 경고를 서버 호출(250ms 디바운스)로 교체. 이미 계약 있는 유입 고객은 덮어쓰기(PATCH).
- D2 금액 인라인 수정: PATCH + 실패 토스트. D3 목표: PUT 응답으로 그리드·이력 동기화.
- D4 분석: 전체 데이터 단일 fetch 후 scope 탭은 클라이언트 전환. D5 기존 고객 목록(계약 집계)·상세.
- 대시보드·보고서의 mock-revenue 사용부는 B3(광고 수집)·B4(보고서)에서 전환 예정 — 이번 범위 밖.

### 검증 (브라우저 실측)
- `tsc --noEmit` 통과, 콘솔 에러 0건.
- D1: 33건·완료 합계 ₩29.26M 렌더 → 직접 입력(기존·신규 등록 '노정민' ₩900,000, POST 201) →
  **하드 리로드 후 유지**(34건·₩30.16M, 연락처 서버 마스킹 010-****-7777).
- 중복 경고: 신규 등록 이름 '한지수' 입력 시 서버 중복 후보 안내 노출 확인.
- D3: 9월 목표 ₩47M 설정 → 리로드 후 그리드·불변 이력 유지. curl로 넣은 8월 ₩45M도 표시(영속 교차 확인).
- D4: 달성률 12일차/31일(실제 오늘), ROAS 0.59배·ROI 55.5%(7월 페이스 환산 광고비 ₩3.9M), 기여도
  구글/메타/기타 — 목데이터 규칙과 동일 판정(증거 없는 네이버 계약은 '기타').
- 한계: 광고비는 monthly_metrics 수기값(B3에서 수집 파이프라인으로 교체). 벌크 상태 변경·CSV 5,000건
  제한·역할별 내보내기 권한은 A1(인증) 이후. 재계산 큐(RecalculateMonth)는 미구현 — 현재는 조회 시 계산.

## 14. 2026-07-12 M3/B3 — 광고 수집 파이프라인 + 대시보드 완전 실데이터

광고 API 커넥터 아키텍처와 수집 파이프라인을 완성하고, 대시보드 3화면(통합 요약·채널별·캠페인별)과
상단 신뢰도 바를 전부 서버 데이터로 전환. **실 광고 API는 심사 대기 중이므로 목 커넥터**(v1 ADS_MOCK
패턴, 결정적 생성)로 파이프라인을 검증 — 심사 통과 시 커넥터 구현체만 교체하면 된다.

### 백엔드 (`mcrm-v2-backend`)
- 스키마(설계 §3.2): `ad_channels`(완결도·토큰 만료 D-5 시나리오), `ad_campaigns`(품질 지표 —
  귀속 엔진 고도화 전 임시), `ad_metrics_daily`(일별×캠페인×키워드, keyword='' 행이 캠페인 합계,
  UNIQUE로 소급 재수집 upsert), `collection_runs`(상태는 저장하지 않고 §1.1.2 규칙으로 파생).
- 파이프라인(§5): `CollectAdMetrics` 잡 — 06:00 KST 3채널 스케줄(routes/console.php), 실패 시
  백오프 5/15/30분×3회. 수동 재수집 최근 7일·하루 3회 제한(4회째 429). 메타 장애 시뮬레이션은
  **정기 수집에만** 적용되어 수동 재수집으로 복구되는 흐름을 재현(ADS_META_OUTAGE env).
- API: `/collection/status`(60초 폴링용)·`/collection/{code}/retry`, `/analytics/channels?period=m1|m3|m6`,
  `/analytics/campaigns`(키워드 드릴다운·매칭/귀속률), `/ad-channels/{code}/campaigns`(S-SSTEUI),
  `/dashboard`(KPI·목표·ROAS 추이·퍼널·채널 비교·비중·신뢰도 일괄 — 실패 채널은 합산 제외+basis 병기 §1.1.1).
- `RevenueMetrics` 공용 서비스 추출 — **매출 분석의 광고비를 수기값(monthly_metrics)에서 수집 지표
  합산으로 교체**(§13의 임시 광고비 해소). 당월은 실비용 기준이라 페이스 환산 제거. YoY 스냅샷(2025-07) 시드.
- 스케줄 실행: `php artisan schedule:work` + `php artisan queue:listen` (로컬).
- 트러블슈팅: Eloquent Collection `except()`는 컬렉션 키가 아닌 **모델 PK** 기준 — 집계(groupBy)
  행에서 전부 제거되는 버그. `->toBase()` 후 사용.

### 프론트 (`mcrm-v2`)
- **`lib/analytics-api.ts`** 신규. `data-trust.tsx` 재작성 — 상태 60초 폴링, retry가 실패 채널을
  자동 타깃, 성공 시 `version` 증가로 소비 화면들이 재조회.
- 통합 요약: 대시보드 컴포넌트 7종(KPI·목표 카드·ROAS 추이·퍼널·채널 비교·비중·신뢰도 요약)을
  하드코딩 시드 → props 기반으로 전환, 페이지가 `/dashboard` 단일 fetch로 공급. 퍼널은 누적 도달
  고객 수 + 최저 전환 구간 하이라이트(전월 대비 증감은 이력 집계 도입 후).
- 상단 신뢰도 바·실패 배너: 채널별 ✓/⚠/✕ 실상태, 배너에 잔여 재시도 횟수 표기.
- 채널별/캠페인별 상세: 기간별(m1/m3/m6) 서버 집계 렌더. C3 수기 입력 캠페인 드롭다운은 채널 선택
  시 서버 조회(마지막 수집 시각 라벨 포함).

### 검증 (브라우저 실측)
- `tsc --noEmit` 통과.
- 초기 상태: 신뢰도 바 "네이버 ✓ · 메타 ✕ 수집 실패 · 구글 ✓", 배너 "마지막 수집 3일 전 ·
  오늘 3회 남음", KPI 광고비 ₩2.2M(수집 성공 2/3 기준)·전환 30건.
- **재시도 클릭 → 메타 복구**: 신뢰도 바 3채널 ✓ "0초 전", 배너 소멸, KPI 자동 재조회
  (광고비 ₩3.5M·3/3, 전환 54건, ROAS 0.73→0.66배 — 메타 포함으로 분모 증가). curl로 4회째 429 확인.
- 채널별 상세(최근 3개월): 합계 ₩23.1M·ROAS 2.89배, 채널별 CTR/CPC/완결도, 월간 스택 막대.
  캠페인별: ROAS 내림차순 7개 캠페인 + 키워드 수·매칭/귀속률 경고색.
- 한계: 목 커넥터(실 API 심사 필요 — 네이버/메타/구글 선신청 권장). 캠페인 품질 지표(매칭/귀속률)와
  채널 완결도는 시드값(귀속 엔진 v2에서 실계산). 퍼널 전월 대비 증감·주간 뷰 미구현. 메타 복구 후
  초기 실패 상태 재현은 `php artisan migrate:fresh --seed`.

## 15. 2026-07-12 네이버 검색광고 실 API 커넥터 (§14 후속)

네이버 검색광고는 심사 없이 API 키를 즉시 발급받을 수 있고 v1(mcrm-backend)에 검증된 연동
(`NaverAdsApiService` + 운영 자격증명)이 이미 있어, 실 커넥터를 바로 이식했다.

- **`NaverAdsConnector`** — v1에서 실계정으로 확인된 제약까지 그대로 이식: /stats의
  timeIncrement(일별 분할)는 11001로 거부 → since=until 캠페인별 개별 호출, 계정당 100 req/s
  제한(429 실측) → Http::pool 청크 20 + 0.3s 간격 + 실패분 1회 재시도. 캠페인 목록 1시간 캐시.
- **`ChannelRoutingConnector`** — 채널별 라우팅: 네이버는 `ADS_NAVER_REAL=true`+자격증명 시 실 API,
  메타/구글은 심사 통과 전까지 목. 자격증명은 v1 .env에서 이관(NAVER_ADS_*, gitignore).
- **점검·전환 커맨드**: `php artisan ads:test-naver`(조회 전용 점검), `php artisan ads:naver-real
  --days=30`(네이버 목 지표 삭제 후 실 데이터 소급 수집 — 목/실 캠페인 혼입 방지).
- **실계정 검증 완료**: 캠페인 11개(인사이트/파워링크#1~6/파워컨텐츠#1/플레이스 등), 최근 3일 일별
  지표(예: 7/10 인사이트 노출 2,090·클릭 2·₩23,167) 정상 조회.
- 주의: 실 캠페인에는 CRM 계약 매칭이 아직 없어 전환 시 네이버 ROAS 분자(확정 귀속 매출)가 0으로
  표시됨 — 귀속 엔진(고객 campaign_name ↔ 실 캠페인 매칭) 도입 후 해소. revenue_attributed를
  플랫폼 전환값으로 채우지 않는 것은 의도(§1.3.4 증거 기반 귀속 원칙).
- 기본값은 `ADS_NAVER_REAL=false`(데모 목 데이터 유지). 실 전환은 .env 변경 + `ads:naver-real` 실행.
