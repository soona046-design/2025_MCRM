# 연동 수정 맵 — 하나를 바꾸면 함께 확인할 곳들

> **용도**: 수정 작업 전에 여기서 진원지를 찾아 체크리스트로 쓴다. 새 연동 관계를 발견하면 이 맵에 추가한다.
> **범례**: 🟢 단일 소스(바꾸면 자동 연동) · 🟡 수동 연동(함께 고쳐야 함) · 🔴 중복 정의(정리 대상)
> 최초 작성 2026-08-13 — 담당자 이원화(§38)·탭 불일치(§44)·메타 하드코딩(§53) 사고의 재발 방지.

## 1. 광고 채널 (채널 추가·API 버전·수집 방식 변경 시)

```
광고 채널 변경
├─ 🟢 수집 상태: GET /collection/status 단일 소스
│   └─ 자동 연동: 상단 신뢰도 바 · 알림(alerts.ts) · 대시보드 조치 카드 ·
│                설정 연동 카드 · 보고서 신뢰도 바/누락 경고 · 채널별 상세 실패 제외
├─ 🟡 config/services.php — API 버전(예: 구글 v23)·키. 버전 폐기 주기 감시 필요
├─ 🟡 routes/console.php — 06:00 수집 스케줄에 채널 추가
├─ 🟢 수기 입력·시트 업로드 — 소스 기준 가드(#48 완료): API 일 단위 있는 (채널·월) 자동 차단/건너뜀
├─ 🔴 채널 색상 4곳 중복: components/dashboard/types.ts(정본) + settings/channels 로컬 +
│      revenue/analytics 로컬 + mock-revenue 내부 — 색 바꾸면 4곳 전부
└─ 🔴 채널명 상수 이원: 백 KoLabels::AD_CHANNELS ↔ 프론트 mock-customers·mock-analytics
```

## 2. 담당자·계정 (계정 추가·역할 변경 시)

```
계정 변경 (설정 › 사용자 권한)
├─ 🟢 담당자 선택지: GET /owners 단일 소스 (§38에서 통일)
│   └─ 자동 연동: 유입·기존 필터, 주 담당자·프로젝트 담당자 셀렉트, 수기 입력 기본값
├─ 🟡 권한 규칙 변경 시: 서버 검증(authorizeAdmin 등) ↔ settings/users의
│      '역할별 권한 안내' 정적 표 — 서버 규칙 바꾸면 표도 수동 갱신
└─ 🟡 역할 목록: KoLabels::ROLES ↔ settings/users ROLE_KEYS (이원 정의)
```

## 3. 메뉴·화면 구조 (메뉴 이동·이름 변경 시)

```
메뉴 변경
├─ 🟢 그룹 하위 = 탭: group-tabs.tsx 단일 소스 (§44에서 통일) — 사이드바가 소비
├─ 🟡 app-shell childActive 정규식 — 상세/휴지통/버튼 흡수 경로의 활성 귀속 규칙
├─ 🟡 페이지 상단 그룹명(브레드크럼) 문구 — 각 page.tsx 하드코딩 (~10곳)
└─ 🟡 안내문 속 메뉴 경로 언급 — "성과 분석 메뉴에서" 등 (grep '메뉴에서\|탭에서')
```

## 4. 매출 인식·지출·미수금 (계산 규칙 변경 시 — #50 착수 전 필독)

```
매출 인식 기준 (현행: status=done + amount NOT NULL + **수금일(paid_at) 월** — 08-13 #50)
├─ 🟢 단일 스코프(#52-3 완료): Contract::revenueRecognized + revenueDateColumn() +
│      scopePaidSoFar — 인식·수금 규칙 변경은 Contract 모델 한 곳에서
├─ 🟡 상태 변경 진입점 2곳(#59): ContractController::update(개별) ↔ bulk(일괄) —
│      완료=수금일 기록 규칙을 바꾸면 양쪽 함께
├─ 🟢 지출 발생 기준: Contract::scopeExpenseRecognized 단일 스코프
│   └─ 소비: RevenueMetrics · MonthlySnapshots · ReportService · RevenueAnalytics(기여도는 인라인 조건 🔴)
├─ 🔴 미수금 정의(invoiced && ongoing) 2곳 하드코딩: DashboardController(조치 카드) ↔
│      revenue/page.tsx(미수금 카드)
└─ 🟡 과거 월 수기 수정 시 MonthlySnapshots::rebuild 호출 — 신규 매출 경로 추가하면 여기도
```

## 5. 고객 단계·보류·전환 (단계 추가·기준 변경 시)

```
단계/보류 변경
├─ 🔴 단계 정의 이원: 백 KoLabels::STAGES ↔ 프론트 mock-customers STAGES
│   └─ 소비: stage_order 파생 · 파이프라인 · adFunnel · C1 칩 · 자동 보류(stage!='contract')
├─ 🔴 "90일" 매직 넘버 산재: 코호트 관찰 창(RevenueAnalyticsController ×2) ·
│      자동 보류(AutoHoldStaleLeads) · 화면 문구 여러 곳 — 기준 바꾸면 전부
├─ 🟡 자동 전환(LeadConversion): 매출 등록(ContractController::store) + 수동 전환 두 진입점
├─ 🟡 보류 제외 집계: 파이프라인(DashboardController) · C1 목록 기본 필터 — 새 집계 추가 시 held 제외 여부 결정
└─ 🟡 재문의 제외 집계(#49): 신규 문의 수·코호트·온라인 유입 퍼널 3곳 — 신규 지표 추가 시 inquiry_type 제외 여부 결정
```

## 6. 유입경로·문의 서비스 분류 (카테고리 추가 시)

```
분류 변경
├─ 🔴 이원 정의: 백 KoLabels(SOURCE_DETAILS·SERVICE_CATEGORIES) ↔ 프론트 mock-customers 동명 상수
├─ 🟢 원본 표기 → 키 분류: ServiceClassifier(서비스) · BackfillSourceDetail::classify(경로)
│      — 임포터·백필 공용. 단, 임포터 내 유입경로 match와 Backfill 규칙은 🟡 2곳 동일 유지 필요
└─ 🟡 필터 UI: C1 목록 셀렉트 · 퍼널 byDetail — 카테고리 추가 시 표시 확인
```

## 7. 정기·혼합형 프로젝트 (회차 생성 규칙 변경 시)

```
정기 규칙 변경
├─ 🟡 회차 생성(ContractController::store recurring 분기) ↔ 월액 변경(RecurringController::changeRate)
│      — 초기 회차 보호 조건이 양쪽에 걸침 (initial_amount + 시작월/적용월 판정)
├─ 🟡 미도래 회차 상태(ongoing) 판정 — 생성 시 날짜 비교 · 수금 진행률(done+도래) 계산과 짝
├─ 🟡 무기한 정기(end_month null, #54): store 당월까지 생성 ↔ contracts:recurring-roll(매일 06:05)
│      매월 자동 생성 — 규칙 바꾸면 양쪽 함께. null 가드 3곳(changeRate 상한·종료 표시·roll 대상)
├─ 🟡 정기 정보 편집(updatePlan): 종료월 변경 시 회차 정리·생성 규칙 — roll·store 생성 규칙과 3곳 세트
├─ 🟢 매출 회차 판정: Contract::scopeRevenueInstallments 단일 스코프(지출 전용 회차 제외)
│      — paidSoFar·정기 total_count 소비. 수금 진행률 규칙 변경은 여기 한 곳
└─ 🟢 유지율: RecurringRetention 단일 서비스 — 정기 관리 카드 + (#57 예정) 주간 성과보고.
       이탈 판정은 종료월 기반 — 해지 시 종료월 입력해야 집계에 잡힘
```

## 8. 지표 정의·표기 (ROAS·수익률·기간 창 변경 시)

```
지표 변경
├─ 🟢 ROAS 표기: fmtRoas 단일 정의(mock-revenue, revenue-api 재수출)
├─ 🟡 Marketing ROAS 정의 문구 — 카드·차트·보고서 라벨 병기 (grep 'Marketing ROAS')
├─ 🟡 3개월 창: 코호트 묶음 · Marketing ROAS 이동값 · 대시보드 채널 위젯 — "같은 창" 정책 유지
└─ 🟡 문서 동기화(상시 규칙): 기능명세서 + 프론트엔드-변경사항 + manyfast PRD + (구조 변경 시) 이 맵
```

## 9. 유입 입력 항목 (수기 입력 필드 추가·변경 시 — #53에서 신설)

```
유입 고객 필드 추가 (예: 기대매출액)
├─ 🟡 백엔드 4곳 세트: 마이그레이션(hasColumn 가드) → Customer Fillable·toFrontend →
│      CustomerController store(validate+create) → update(validate+diff+ChangeLog 이력)
├─ 🟡 프론트 타입 3곳: mock-customers Customer·NewCustomerInput → customers-api CustomerPatch
├─ 🟡 화면 3곳: C3 수기 입력 폼 → C2 상세(표시 + 편집 다이얼로그 state·reset·buildPatch)
│      → C4 파일 업로드 필드 매핑(선택 — 수기 판단 필드는 CSV 대상에서 제외 가능, 기대매출액은 제외)
├─ 🟡 집계 노출 시: index metrics(서버 집계 — 페이지네이션이라 클라 합산 불가) + C1 상단 카드
└─ 🟢 기대매출액 원칙: 유입관리 전용 추정치 — 매출관리 화면·집계에 절대 미포함
       (허용 예외는 계약 등록 다이얼로그의 참고 배지·프리필 한 곳 · 이중 계상 방지)
```

## 정리 대상(🔴) 요약 — 별도 정리 작업 후보
1. 채널 색상 4곳 → types.ts 단일 소스화
2. 채널명·단계·분류 상수의 백/프론트 이원 정의 → 프론트가 API 라벨을 신뢰하는 구조로 (또는 codegen)
3. 매출 인식 조건 8곳 → Contract 스코프(예: scopeRevenueRecognized) 단일화 — #50 착수 시 함께
4. 미수금 정의 2곳 → 서버 단일 계산
5. "90일" 상수 → config 또는 공용 상수
