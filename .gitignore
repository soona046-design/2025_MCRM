.env.deploy
.DS_Store
node_modules
*.tar.gz

# v2 frontend (nested git repo, own remote)
mcrm-v2/

# v2 backend (nested git repo)
mcrm-v2-backend/

# DesignSync 도구 산출물 (Claude Design 프로젝트가 원본)
.ds-sync/
ds-bundle/

# 실데이터 원본 (개인정보 포함 — 커밋 금지). 서식(templates)·README는 커밋됨
data/original/
